"""
Complex Scenario Generator for Ambiguous Compliance Testing

Generates edge cases and ambiguous audits where simple rule-based logic struggles.
Tests quantum superposition advantage in complex decision spaces.

PDA Loop + AfterMath:
- PLAN: Define complexity dimensions
- DO: Generate ambiguous scenarios
- ASSESS: Validate scenario diversity
- AfterMath: Track which scenarios challenge classical approach
"""

import random
from dataclasses import dataclass
from typing import List, Tuple

from cognitive_brain.integrations.compliance_integration import (
    AuditResult,
    ComplianceDecision,
)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@dataclass
class ScenarioComplexity:
    """Metrics describing scenario complexity"""

    ambiguity_score: float  # 0-1, higher = more ambiguous
    conflicting_signals: int  # Number of conflicting indicators
    rule_coverage: float  # 0-1, how well rules cover this case


def x_generate_complex_scenarios__mutmut_orig(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_1(
    count: int, seed: int = 43
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_2(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = None  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_3(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(None)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_4(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = None

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_5(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(None):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_6(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(None)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_7(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count / 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_8(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 1.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_9(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = None  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_10(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(None, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_11(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, None)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_12(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_13(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, )  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_14(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(1.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_15(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 1.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_16(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = None
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_17(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_18(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=None,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_19(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level=None,  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_20(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=None,  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_21(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=None,  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_22(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=None,
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_23(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_24(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_25(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_26(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_27(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_28(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_29(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="XXhighXX",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_30(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="HIGH",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_31(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(None, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_32(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, None),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_33(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_34(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, ),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_35(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5001, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_36(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15001),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_37(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(None, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_38(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, None),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_39(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_40(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, ),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_41(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(1.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_42(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 1.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_43(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(None)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_44(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(3)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_45(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_46(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_47(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=None,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_48(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=None,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_49(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=None,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_50(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_51(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_52(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_53(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=1.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_54(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=3,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_55(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=1.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_56(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append(None)

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_57(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(None):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_58(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(None)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_59(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count / 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_60(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 1.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_61(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = None  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_62(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(None, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_63(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, None)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_64(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_65(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, )  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_66(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(1.4, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_67(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 1.6)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_68(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = None
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_69(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_70(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=None,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_71(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=None,
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_72(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=None,  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_73(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=None,  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_74(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=None,
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_75(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_76(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_77(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_78(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_79(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_80(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_81(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(None),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_82(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["XXlowXX", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_83(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["LOW", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_84(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "XXmediumXX"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_85(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "MEDIUM"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_86(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(None, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_87(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, None),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_88(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_89(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, ),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_90(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(501, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_91(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2001),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_92(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(None, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_93(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, None),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_94(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_95(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, ),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_96(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(1.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_97(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 1.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_98(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(None)],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_99(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(None, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_100(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, None))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_101(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_102(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, ))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_103(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(3, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_104(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_105(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_106(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost <= 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_107(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1501
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_108(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_109(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=None,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_110(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=None,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_111(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=None,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_112(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_113(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_114(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_115(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=1.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_116(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=3,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_117(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=1.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_118(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append(None)

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_119(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(None):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_120(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(None)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_121(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count / 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_122(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 1.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_123(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = None  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_124(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(None, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_125(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, None)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_126(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_127(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, )  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_128(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(1.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_129(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 1.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_130(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = None
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_131(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_132(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=None,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_133(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level=None,
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_134(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=None,
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_135(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=None,
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_136(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=None,
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_137(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_138(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_139(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_140(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_141(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_142(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_143(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="XXmediumXX",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_144(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="MEDIUM",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_145(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(None, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_146(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, None),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_147(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_148(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, ),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_149(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2001, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_150(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5001),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_151(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(None, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_152(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, None),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_153(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_154(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, ),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_155(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(1.5, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_156(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 1.7),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_157(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(None)],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_158(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(None, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_159(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, None))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_160(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_161(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, ))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_162(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(3, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_163(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 6))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_164(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 or audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_165(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score >= 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_166(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 1.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_167(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact >= 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_168(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 1.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_169(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = None
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_170(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost <= 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_171(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3001:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_172(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_173(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_174(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_175(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=None,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_176(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=None,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_177(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=None,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_178(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_179(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_180(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_181(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=1.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_182(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=1,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_183(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=1.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_184(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append(None)

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_185(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(None):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_186(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(None)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_187(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count / 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_188(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 1.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_189(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = None  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_190(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice(None)  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_191(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([1.5, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_192(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 1.7, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_193(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 1.9])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_194(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = None
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_195(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary - _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_196(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(None, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_197(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, None)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_198(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_199(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, )
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_200(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(+0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_201(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-1.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_202(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 1.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_203(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = None
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_204(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_205(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=None,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_206(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=None,
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_207(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=None,  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_208(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=None,  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_209(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=None,
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_210(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_211(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_212(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_213(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_214(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_215(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_216(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(None),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_217(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["XXlowXX", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_218(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["LOW", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_219(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "XXmediumXX", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_220(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "MEDIUM", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_221(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "XXhighXX"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_222(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "HIGH"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_223(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 - _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_224(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2001 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_225(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(None, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_226(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, None),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_227(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_228(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, ),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_229(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(+100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_230(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-101, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_231(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 101),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_232(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(None, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_233(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, None),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_234(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_235(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, ),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_236(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(1.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_237(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 1.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_238(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(None)],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_239(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(None, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_240(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, None))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_241(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_242(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, ))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_243(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(2, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_244(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 7))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_245(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score > 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_246(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 1.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_247(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = None
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_248(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level != "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_249(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "XXlowXX"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_250(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "LOW"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_251(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score > 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_252(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 1.6800000000000002:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_253(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = None
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_254(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost <= 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_255(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2101:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_256(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_257(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_258(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_259(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=None, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_260(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=None, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_261(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=None
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_262(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_263(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_264(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_265(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=1.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_266(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=2, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_267(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=1.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_268(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append(None)

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_269(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(None):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_270(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(None)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_271(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count / 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_272(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 1.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_273(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = None  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_274(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(None, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_275(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, None)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_276(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_277(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, )  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_278(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(1.6, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_279(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 1.8)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_280(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = None
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_281(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(None, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_282(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, None)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_283(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_284(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, )
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_285(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(2, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_286(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 4)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_287(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = None
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_288(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_289(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=None,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_290(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=None,
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_291(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=None,
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_292(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=None,
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_293(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=None,
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_294(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_295(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_296(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_297(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_298(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_299(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_300(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(None),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_301(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["XXmediumXX", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_302(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["MEDIUM", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_303(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "XXhighXX"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_304(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "HIGH"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_305(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(None, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_306(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, None),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_307(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_308(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, ),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_309(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3001, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_310(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8001),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_311(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(None, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_312(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, None),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_313(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_314(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, ),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_315(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(1.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_316(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 1.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_317(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(None)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_318(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 and audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_319(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators > 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_320(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 4 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_321(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level != "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_322(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "XXhighXX":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_323(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "HIGH":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_324(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = None
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_325(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost <= 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_326(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5001:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_327(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_328(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_329(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_330(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=None,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_331(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=None,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_332(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=None,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_333(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_334(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_335(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_336(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=1.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_337(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=3,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_338(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=1.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_339(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append(None)

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_340(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(None):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_341(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(None)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_342(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count / 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_343(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 1.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_344(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = None
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_345(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(None, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_346(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, None)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_347(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_348(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, )
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_349(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(1.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_350(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 1.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_351(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = None  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_352(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(None, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_353(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, None)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_354(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_355(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, )  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_356(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(4, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_357(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 8)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_358(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = None
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_359(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_360(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=None,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_361(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=None,
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_362(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=None,
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_363(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=None,
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_364(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=None,
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_365(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_366(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_367(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_368(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_369(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_370(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_371(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(None),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_372(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["XXlowXX", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_373(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["LOW", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_374(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "XXmediumXX", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_375(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "MEDIUM", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_376(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "XXhighXX"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_377(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "HIGH"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_378(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(None, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_379(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, None),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_380(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_381(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, ),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_382(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1001, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_383(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10001),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_384(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(None, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_385(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, None),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_386(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_387(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, ),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_388(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(1.4, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_389(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 1.9),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_390(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j / 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_391(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 5}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_392(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(None)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_393(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = None
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_394(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count / (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_395(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score) / violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_396(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 + score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_397(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (2.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_398(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (2.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_399(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level != "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_400(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "XXhighXX" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_401(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "HIGH" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_402(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 1.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_403(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score >= 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_404(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 5.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_405(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = None
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_406(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score >= 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_407(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 3.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_408(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = None
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_409(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact >= 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_410(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 1.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_411(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_412(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_413(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_414(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=None,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_415(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=None,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_416(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=None,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_417(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_418(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_419(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_420(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=1.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_421(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=4,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_422(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=1.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_423(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append(None)

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_424(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(None):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_425(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(None)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_426(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count / 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_427(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 1.1)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_428(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = None  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_429(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(None, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_430(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, None)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_431(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_432(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, )  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_433(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(1.8, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_434(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 1.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_435(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = None
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_436(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_437(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=None,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_438(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level=None,  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_439(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=None,  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_440(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=None,  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_441(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=None,
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_442(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_443(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_444(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_445(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_446(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_447(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_448(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="XXhighXX",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_449(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="HIGH",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_450(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(None, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_451(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, None),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_452(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_453(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, ),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_454(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10001, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_455(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20001),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_456(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(None, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_457(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, None),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_458(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_459(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, ),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_460(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(1.7, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_461(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 1.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_462(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["XXSecurityVulnerabilityXX", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_463(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["securityvulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_464(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SECURITYVULNERABILITY", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_465(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "XXHighRiskExposureXX"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_466(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "highriskexposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_467(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HIGHRISKEXPOSURE"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_468(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost <= 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_469(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15001:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_470(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_471(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_472(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_473(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=None,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_474(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=None,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_475(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=None,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_476(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_477(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_478(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_479(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=1.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_480(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=4,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_481(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=1.2,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_482(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append(None)

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_483(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(None):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_484(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(None)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_485(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count / 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_486(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 1.1)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_487(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = None
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_488(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(None, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_489(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, None)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_490(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_491(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, )
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_492(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(1.5, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_493(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 1.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_494(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = None
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_495(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(None, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_496(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, None)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_497(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_498(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, )
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_499(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(1.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_500(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 2.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_501(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = None

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_502(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(None, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_503(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, None)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_504(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_505(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, )

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_506(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(2.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_507(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score / temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_508(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = None
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_509(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=None,
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_510(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=None,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_511(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=None,
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_512(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=None,
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_513(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=None,
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_514(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=None,
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_515(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_516(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_517(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_518(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_519(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_520(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_521(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(None),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_522(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["XXlowXX", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_523(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["LOW", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_524(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "XXmediumXX", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_525(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "MEDIUM", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_526(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "XXhighXX"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_527(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "HIGH"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_528(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(None, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_529(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, None),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_530(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_531(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, ),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_532(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2001, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_533(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12001),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_534(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(None, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_535(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, None),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_536(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_537(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, ),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_538(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(1.5, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_539(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 1.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_540(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(None)],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_541(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(None, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_542(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, None))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_543(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_544(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, ))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_545(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_546(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 5))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_547(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score > 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_548(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 1.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_549(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = None
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_550(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score > 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_551(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 1.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_552(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = None
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_553(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost <= 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_554(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6001:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_555(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_556(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_557(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_558(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=None,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_559(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=None,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_560(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=None,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_561(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_562(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_563(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_564(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=1.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_565(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=3,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_566(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=1.3,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_567(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append(None)

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_568(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) <= count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_569(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = None
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_570(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = None
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_571(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice(None)
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_572(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([1.5, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_573(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 1.7, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_574(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 1.9])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_575(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = None
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_576(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary - _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_577(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(None, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_578(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, None)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_579(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_580(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, )
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_581(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(+0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_582(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-1.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_583(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 1.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_584(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = None
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_585(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=None,
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_586(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=None,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_587(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=None,
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_588(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=None,
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_589(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=None,
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_590(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=None,
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_591(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_592(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_593(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_594(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_595(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_596(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_597(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(None),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_598(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["XXlowXX", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_599(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["LOW", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_600(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "XXmediumXX", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_601(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "MEDIUM", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_602(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "XXhighXX"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_603(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "HIGH"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_604(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 - _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_605(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2001 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_606(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(None, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_607(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, None),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_608(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_609(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, ),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_610(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(+100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_611(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-101, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_612(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 101),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_613(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(None, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_614(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, None),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_615(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_616(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, ),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_617(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(1.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_618(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 1.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_619(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(None)],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_620(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(None, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_621(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, None))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_622(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_623(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, ))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_624(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(2, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_625(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_626(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score > 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_627(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 1.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_628(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = None
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_629(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level != "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_630(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "XXlowXX"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_631(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "LOW"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_632(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score > 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_633(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 1.6800000000000002:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_634(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = None
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_635(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost <= 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_636(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2101:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_637(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = None
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_638(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = None
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_639(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = None
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_640(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=None, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_641(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=None, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_642(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=None
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_643(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_644(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_645(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_646(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=1.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_647(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=2, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_648(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=1.6
        )
        scenarios.append((audit, ground_truth, complexity))

    return scenarios


def x_generate_complex_scenarios__mutmut_649(
    count: int, seed: int = 42
) -> List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]]:
    """
    Generate complex, ambiguous compliance scenarios.

    These scenarios have conflicting signals that make simple rule-based
    decisions difficult, testing quantum superposition's advantage.

    Phase 8.0 Expansion: Now generates up to 100 scenarios with enhanced patterns:
    1. High compliance + high risk (conflicting) - 15%
    2. Low compliance + high business impact (tradeoff) - 15%
    3. Medium everything (ambiguous) - 15%
    4. Marginal boundaries (edge cases) - 15%
    5. Ambiguous PII exposure cases - 15%
    6. Multi-violation interactions - 15%
    7. Compliance vs security conflicts - 10%
    8. Temporal complexity (evolving violations) - 10%

    Args:
        count: Number of scenarios to generate (recommended: 100 for Phase 8.0)
        seed: Random seed for reproducibility

    Returns:
        List of (audit, ground_truth, complexity) tuples
    """
    # Copilot: Using random.Random() instance for reproducible test data generation.
    # This is NOT for security/cryptographic purposes - Bandit B311 is a false positive here.
    # These are experiment scenarios for testing compliance decision algorithms.
    _rng = random.Random(seed)  # nosec B311
    scenarios = []

    # Pattern 1: High compliance + high risk (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.75, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-A-{i}",
            score=score,
            risk_level="high",  # But high risk!
            remediation_cost=_rng.uniform(5000, 15000),  # Expensive
            business_impact=_rng.uniform(0.6, 0.9),  # Good impact
            violations=[f"HighRiskViolation-{j}" for j in range(2)],
        )
        # Ground truth: CONDITIONAL due to risk despite good score
        ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.8,
            conflicting_signals=2,  # Score vs risk
            rule_coverage=0.4,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 2: Low compliance + high impact (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.40, 0.60)  # Low-medium compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-B-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium"]),
            remediation_cost=_rng.uniform(500, 2000),  # Cheap to fix
            business_impact=_rng.uniform(0.85, 0.98),  # Very high impact!
            violations=[f"MinorViolation-{j}" for j in range(_rng.randint(2, 4))],
        )
        # Ground truth: CONDITIONAL or MONITOR depending on fix cost
        ground_truth = (
            ComplianceDecision.CONDITIONAL_APPROVAL
            if audit.remediation_cost < 1500
            else ComplianceDecision.APPROVE_WITH_MONITORING
        )
        complexity = ScenarioComplexity(
            ambiguity_score=0.75,
            conflicting_signals=2,  # Compliance vs impact
            rule_coverage=0.5,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 3: Everything medium (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.55, 0.75)  # Medium
        audit = AuditResult(
            audit_id=f"COMPLEX-C-{i}",
            score=score,
            risk_level="medium",
            remediation_cost=_rng.uniform(2000, 5000),
            business_impact=_rng.uniform(0.50, 0.70),
            violations=[f"MediumViolation-{j}" for j in range(_rng.randint(2, 5))],
        )
        # Ground truth: Very ambiguous, use weighted decision
        if score > 0.65 and audit.business_impact > 0.6:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 3000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.9,  # Very ambiguous
            conflicting_signals=0,  # No clear conflicts
            rule_coverage=0.3,  # Rules don't cover well
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 4: Boundary cases (15%)
    for i in range(int(count * 0.15)):
        # Right on decision boundaries
        score_boundary = _rng.choice([0.50, 0.70, 0.90])  # Key thresholds
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-D-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),  # Near threshold
            business_impact=_rng.uniform(0.45, 0.55),  # Near midpoint
            violations=[f"BoundaryViolation-{j}" for j in range(_rng.randint(1, 6))],
        )
        # Apply boundary logic
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 5: Ambiguous PII exposure cases (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.60, 0.80)  # Medium-high
        # Ambiguous: might contain PII but unclear
        pii_indicators = _rng.randint(1, 3)
        audit = AuditResult(
            audit_id=f"COMPLEX-E-{i}",
            score=score,
            risk_level=_rng.choice(["medium", "high"]),
            remediation_cost=_rng.uniform(3000, 8000),
            business_impact=_rng.uniform(0.55, 0.85),
            violations=[f"PotentialPII-{j}" for j in range(pii_indicators)],
        )
        # Ground truth: depends on PII likelihood and remediation cost
        if pii_indicators >= 3 or audit.risk_level == "high":
            ground_truth = ComplianceDecision.REJECT
        elif audit.remediation_cost < 5000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.85,
            conflicting_signals=2,  # PII vs business value
            rule_coverage=0.35,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 6: Multi-violation interaction scenarios (15%)
    for i in range(int(count * 0.15)):
        score = _rng.uniform(0.45, 0.75)
        violation_count = _rng.randint(3, 7)  # Multiple violations
        audit = AuditResult(
            audit_id=f"COMPLEX-F-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(1000, 10000),
            business_impact=_rng.uniform(0.40, 0.90),
            violations=[f"Violation-Type{j % 4}-{j}" for j in range(violation_count)],
        )
        # Ground truth: complex interaction of multiple factors
        severity_score = (
            (1.0 - score)
            * violation_count
            * (1.0 if audit.risk_level == "high" else 0.5)
        )
        if severity_score > 4.0:
            ground_truth = ComplianceDecision.REJECT
        elif severity_score > 2.5:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.business_impact > 0.7:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        else:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        complexity = ScenarioComplexity(
            ambiguity_score=0.88,
            conflicting_signals=3,  # Multiple interacting factors
            rule_coverage=0.25,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 7: Compliance vs security conflict edge cases (10%)
    for i in range(int(count * 0.10)):
        score = _rng.uniform(0.80, 0.95)  # High compliance
        audit = AuditResult(
            audit_id=f"COMPLEX-G-{i}",
            score=score,
            risk_level="high",  # But high security risk!
            remediation_cost=_rng.uniform(10000, 20000),  # Very expensive
            business_impact=_rng.uniform(0.70, 0.95),  # High business value
            violations=["SecurityVulnerability", "HighRiskExposure"],
        )
        # Ground truth: security trumps compliance in most cases
        if audit.remediation_cost < 15000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        complexity = ScenarioComplexity(
            ambiguity_score=0.92,
            conflicting_signals=3,  # Compliance vs security vs cost vs impact
            rule_coverage=0.20,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Pattern 8: Temporal complexity (evolving violations over time) (10%)
    for i in range(int(count * 0.10)):
        base_score = _rng.uniform(0.50, 0.85)
        # Violations that change severity over time (factor 0.5-1.5)
        # Business Logic: Models real-world compliance score evolution
        #   - Deterioration (0.5-1.0): Security patches expire, policies change, drift occurs
        #   - Improvement (1.0-1.5): Remediation efforts, new controls, compliance catch-up
        # Symmetric range around 1.0 allows equal probability of improvement/deterioration
        temporal_factor = _rng.uniform(0.5, 1.5)
        # Cap adjusted score at 1.0 to ensure valid range (0.0-1.0)
        adjusted_score = min(1.0, base_score * temporal_factor)

        audit = AuditResult(
            audit_id=f"COMPLEX-H-{i}",
            score=adjusted_score,  # Score affected by time, capped at 1.0
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=_rng.uniform(2000, 12000),
            business_impact=_rng.uniform(0.50, 0.85),
            violations=[f"EvolvingViolation-{j}" for j in range(_rng.randint(1, 4))],
        )
        # Ground truth: use adjusted score for consistency
        if adjusted_score >= 0.85:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif adjusted_score >= 0.65:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        elif audit.remediation_cost < 6000:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.87,
            conflicting_signals=2,  # Current state vs future evolution
            rule_coverage=0.30,
        )
        scenarios.append((audit, ground_truth, complexity))

    # Fill remaining to exact count (if needed due to rounding)
    while len(scenarios) < count:
        # Add boundary cases
        i = len(scenarios)
        score_boundary = _rng.choice([0.50, 0.70, 0.90])
        score = score_boundary + _rng.uniform(-0.02, 0.02)
        audit = AuditResult(
            audit_id=f"COMPLEX-FILL-{i}",
            score=score,
            risk_level=_rng.choice(["low", "medium", "high"]),
            remediation_cost=2000 + _rng.uniform(-100, 100),
            business_impact=_rng.uniform(0.45, 0.55),
            violations=[f"FillViolation-{j}" for j in range(_rng.randint(1, 3))],
        )
        if score >= 0.88:
            ground_truth = (
                ComplianceDecision.APPROVE
                if audit.risk_level == "low"
                else ComplianceDecision.APPROVE_WITH_MONITORING
            )
        elif score >= 0.68:
            ground_truth = ComplianceDecision.APPROVE_WITH_MONITORING
        elif audit.remediation_cost < 2100:
            ground_truth = ComplianceDecision.CONDITIONAL_APPROVAL
        else:
            ground_truth = ComplianceDecision.REJECT
        complexity = ScenarioComplexity(
            ambiguity_score=0.85, conflicting_signals=1, rule_coverage=0.6
        )
        scenarios.append(None)

    return scenarios

x_generate_complex_scenarios__mutmut_mutants : ClassVar[MutantDict] = {
'x_generate_complex_scenarios__mutmut_1': x_generate_complex_scenarios__mutmut_1, 
    'x_generate_complex_scenarios__mutmut_2': x_generate_complex_scenarios__mutmut_2, 
    'x_generate_complex_scenarios__mutmut_3': x_generate_complex_scenarios__mutmut_3, 
    'x_generate_complex_scenarios__mutmut_4': x_generate_complex_scenarios__mutmut_4, 
    'x_generate_complex_scenarios__mutmut_5': x_generate_complex_scenarios__mutmut_5, 
    'x_generate_complex_scenarios__mutmut_6': x_generate_complex_scenarios__mutmut_6, 
    'x_generate_complex_scenarios__mutmut_7': x_generate_complex_scenarios__mutmut_7, 
    'x_generate_complex_scenarios__mutmut_8': x_generate_complex_scenarios__mutmut_8, 
    'x_generate_complex_scenarios__mutmut_9': x_generate_complex_scenarios__mutmut_9, 
    'x_generate_complex_scenarios__mutmut_10': x_generate_complex_scenarios__mutmut_10, 
    'x_generate_complex_scenarios__mutmut_11': x_generate_complex_scenarios__mutmut_11, 
    'x_generate_complex_scenarios__mutmut_12': x_generate_complex_scenarios__mutmut_12, 
    'x_generate_complex_scenarios__mutmut_13': x_generate_complex_scenarios__mutmut_13, 
    'x_generate_complex_scenarios__mutmut_14': x_generate_complex_scenarios__mutmut_14, 
    'x_generate_complex_scenarios__mutmut_15': x_generate_complex_scenarios__mutmut_15, 
    'x_generate_complex_scenarios__mutmut_16': x_generate_complex_scenarios__mutmut_16, 
    'x_generate_complex_scenarios__mutmut_17': x_generate_complex_scenarios__mutmut_17, 
    'x_generate_complex_scenarios__mutmut_18': x_generate_complex_scenarios__mutmut_18, 
    'x_generate_complex_scenarios__mutmut_19': x_generate_complex_scenarios__mutmut_19, 
    'x_generate_complex_scenarios__mutmut_20': x_generate_complex_scenarios__mutmut_20, 
    'x_generate_complex_scenarios__mutmut_21': x_generate_complex_scenarios__mutmut_21, 
    'x_generate_complex_scenarios__mutmut_22': x_generate_complex_scenarios__mutmut_22, 
    'x_generate_complex_scenarios__mutmut_23': x_generate_complex_scenarios__mutmut_23, 
    'x_generate_complex_scenarios__mutmut_24': x_generate_complex_scenarios__mutmut_24, 
    'x_generate_complex_scenarios__mutmut_25': x_generate_complex_scenarios__mutmut_25, 
    'x_generate_complex_scenarios__mutmut_26': x_generate_complex_scenarios__mutmut_26, 
    'x_generate_complex_scenarios__mutmut_27': x_generate_complex_scenarios__mutmut_27, 
    'x_generate_complex_scenarios__mutmut_28': x_generate_complex_scenarios__mutmut_28, 
    'x_generate_complex_scenarios__mutmut_29': x_generate_complex_scenarios__mutmut_29, 
    'x_generate_complex_scenarios__mutmut_30': x_generate_complex_scenarios__mutmut_30, 
    'x_generate_complex_scenarios__mutmut_31': x_generate_complex_scenarios__mutmut_31, 
    'x_generate_complex_scenarios__mutmut_32': x_generate_complex_scenarios__mutmut_32, 
    'x_generate_complex_scenarios__mutmut_33': x_generate_complex_scenarios__mutmut_33, 
    'x_generate_complex_scenarios__mutmut_34': x_generate_complex_scenarios__mutmut_34, 
    'x_generate_complex_scenarios__mutmut_35': x_generate_complex_scenarios__mutmut_35, 
    'x_generate_complex_scenarios__mutmut_36': x_generate_complex_scenarios__mutmut_36, 
    'x_generate_complex_scenarios__mutmut_37': x_generate_complex_scenarios__mutmut_37, 
    'x_generate_complex_scenarios__mutmut_38': x_generate_complex_scenarios__mutmut_38, 
    'x_generate_complex_scenarios__mutmut_39': x_generate_complex_scenarios__mutmut_39, 
    'x_generate_complex_scenarios__mutmut_40': x_generate_complex_scenarios__mutmut_40, 
    'x_generate_complex_scenarios__mutmut_41': x_generate_complex_scenarios__mutmut_41, 
    'x_generate_complex_scenarios__mutmut_42': x_generate_complex_scenarios__mutmut_42, 
    'x_generate_complex_scenarios__mutmut_43': x_generate_complex_scenarios__mutmut_43, 
    'x_generate_complex_scenarios__mutmut_44': x_generate_complex_scenarios__mutmut_44, 
    'x_generate_complex_scenarios__mutmut_45': x_generate_complex_scenarios__mutmut_45, 
    'x_generate_complex_scenarios__mutmut_46': x_generate_complex_scenarios__mutmut_46, 
    'x_generate_complex_scenarios__mutmut_47': x_generate_complex_scenarios__mutmut_47, 
    'x_generate_complex_scenarios__mutmut_48': x_generate_complex_scenarios__mutmut_48, 
    'x_generate_complex_scenarios__mutmut_49': x_generate_complex_scenarios__mutmut_49, 
    'x_generate_complex_scenarios__mutmut_50': x_generate_complex_scenarios__mutmut_50, 
    'x_generate_complex_scenarios__mutmut_51': x_generate_complex_scenarios__mutmut_51, 
    'x_generate_complex_scenarios__mutmut_52': x_generate_complex_scenarios__mutmut_52, 
    'x_generate_complex_scenarios__mutmut_53': x_generate_complex_scenarios__mutmut_53, 
    'x_generate_complex_scenarios__mutmut_54': x_generate_complex_scenarios__mutmut_54, 
    'x_generate_complex_scenarios__mutmut_55': x_generate_complex_scenarios__mutmut_55, 
    'x_generate_complex_scenarios__mutmut_56': x_generate_complex_scenarios__mutmut_56, 
    'x_generate_complex_scenarios__mutmut_57': x_generate_complex_scenarios__mutmut_57, 
    'x_generate_complex_scenarios__mutmut_58': x_generate_complex_scenarios__mutmut_58, 
    'x_generate_complex_scenarios__mutmut_59': x_generate_complex_scenarios__mutmut_59, 
    'x_generate_complex_scenarios__mutmut_60': x_generate_complex_scenarios__mutmut_60, 
    'x_generate_complex_scenarios__mutmut_61': x_generate_complex_scenarios__mutmut_61, 
    'x_generate_complex_scenarios__mutmut_62': x_generate_complex_scenarios__mutmut_62, 
    'x_generate_complex_scenarios__mutmut_63': x_generate_complex_scenarios__mutmut_63, 
    'x_generate_complex_scenarios__mutmut_64': x_generate_complex_scenarios__mutmut_64, 
    'x_generate_complex_scenarios__mutmut_65': x_generate_complex_scenarios__mutmut_65, 
    'x_generate_complex_scenarios__mutmut_66': x_generate_complex_scenarios__mutmut_66, 
    'x_generate_complex_scenarios__mutmut_67': x_generate_complex_scenarios__mutmut_67, 
    'x_generate_complex_scenarios__mutmut_68': x_generate_complex_scenarios__mutmut_68, 
    'x_generate_complex_scenarios__mutmut_69': x_generate_complex_scenarios__mutmut_69, 
    'x_generate_complex_scenarios__mutmut_70': x_generate_complex_scenarios__mutmut_70, 
    'x_generate_complex_scenarios__mutmut_71': x_generate_complex_scenarios__mutmut_71, 
    'x_generate_complex_scenarios__mutmut_72': x_generate_complex_scenarios__mutmut_72, 
    'x_generate_complex_scenarios__mutmut_73': x_generate_complex_scenarios__mutmut_73, 
    'x_generate_complex_scenarios__mutmut_74': x_generate_complex_scenarios__mutmut_74, 
    'x_generate_complex_scenarios__mutmut_75': x_generate_complex_scenarios__mutmut_75, 
    'x_generate_complex_scenarios__mutmut_76': x_generate_complex_scenarios__mutmut_76, 
    'x_generate_complex_scenarios__mutmut_77': x_generate_complex_scenarios__mutmut_77, 
    'x_generate_complex_scenarios__mutmut_78': x_generate_complex_scenarios__mutmut_78, 
    'x_generate_complex_scenarios__mutmut_79': x_generate_complex_scenarios__mutmut_79, 
    'x_generate_complex_scenarios__mutmut_80': x_generate_complex_scenarios__mutmut_80, 
    'x_generate_complex_scenarios__mutmut_81': x_generate_complex_scenarios__mutmut_81, 
    'x_generate_complex_scenarios__mutmut_82': x_generate_complex_scenarios__mutmut_82, 
    'x_generate_complex_scenarios__mutmut_83': x_generate_complex_scenarios__mutmut_83, 
    'x_generate_complex_scenarios__mutmut_84': x_generate_complex_scenarios__mutmut_84, 
    'x_generate_complex_scenarios__mutmut_85': x_generate_complex_scenarios__mutmut_85, 
    'x_generate_complex_scenarios__mutmut_86': x_generate_complex_scenarios__mutmut_86, 
    'x_generate_complex_scenarios__mutmut_87': x_generate_complex_scenarios__mutmut_87, 
    'x_generate_complex_scenarios__mutmut_88': x_generate_complex_scenarios__mutmut_88, 
    'x_generate_complex_scenarios__mutmut_89': x_generate_complex_scenarios__mutmut_89, 
    'x_generate_complex_scenarios__mutmut_90': x_generate_complex_scenarios__mutmut_90, 
    'x_generate_complex_scenarios__mutmut_91': x_generate_complex_scenarios__mutmut_91, 
    'x_generate_complex_scenarios__mutmut_92': x_generate_complex_scenarios__mutmut_92, 
    'x_generate_complex_scenarios__mutmut_93': x_generate_complex_scenarios__mutmut_93, 
    'x_generate_complex_scenarios__mutmut_94': x_generate_complex_scenarios__mutmut_94, 
    'x_generate_complex_scenarios__mutmut_95': x_generate_complex_scenarios__mutmut_95, 
    'x_generate_complex_scenarios__mutmut_96': x_generate_complex_scenarios__mutmut_96, 
    'x_generate_complex_scenarios__mutmut_97': x_generate_complex_scenarios__mutmut_97, 
    'x_generate_complex_scenarios__mutmut_98': x_generate_complex_scenarios__mutmut_98, 
    'x_generate_complex_scenarios__mutmut_99': x_generate_complex_scenarios__mutmut_99, 
    'x_generate_complex_scenarios__mutmut_100': x_generate_complex_scenarios__mutmut_100, 
    'x_generate_complex_scenarios__mutmut_101': x_generate_complex_scenarios__mutmut_101, 
    'x_generate_complex_scenarios__mutmut_102': x_generate_complex_scenarios__mutmut_102, 
    'x_generate_complex_scenarios__mutmut_103': x_generate_complex_scenarios__mutmut_103, 
    'x_generate_complex_scenarios__mutmut_104': x_generate_complex_scenarios__mutmut_104, 
    'x_generate_complex_scenarios__mutmut_105': x_generate_complex_scenarios__mutmut_105, 
    'x_generate_complex_scenarios__mutmut_106': x_generate_complex_scenarios__mutmut_106, 
    'x_generate_complex_scenarios__mutmut_107': x_generate_complex_scenarios__mutmut_107, 
    'x_generate_complex_scenarios__mutmut_108': x_generate_complex_scenarios__mutmut_108, 
    'x_generate_complex_scenarios__mutmut_109': x_generate_complex_scenarios__mutmut_109, 
    'x_generate_complex_scenarios__mutmut_110': x_generate_complex_scenarios__mutmut_110, 
    'x_generate_complex_scenarios__mutmut_111': x_generate_complex_scenarios__mutmut_111, 
    'x_generate_complex_scenarios__mutmut_112': x_generate_complex_scenarios__mutmut_112, 
    'x_generate_complex_scenarios__mutmut_113': x_generate_complex_scenarios__mutmut_113, 
    'x_generate_complex_scenarios__mutmut_114': x_generate_complex_scenarios__mutmut_114, 
    'x_generate_complex_scenarios__mutmut_115': x_generate_complex_scenarios__mutmut_115, 
    'x_generate_complex_scenarios__mutmut_116': x_generate_complex_scenarios__mutmut_116, 
    'x_generate_complex_scenarios__mutmut_117': x_generate_complex_scenarios__mutmut_117, 
    'x_generate_complex_scenarios__mutmut_118': x_generate_complex_scenarios__mutmut_118, 
    'x_generate_complex_scenarios__mutmut_119': x_generate_complex_scenarios__mutmut_119, 
    'x_generate_complex_scenarios__mutmut_120': x_generate_complex_scenarios__mutmut_120, 
    'x_generate_complex_scenarios__mutmut_121': x_generate_complex_scenarios__mutmut_121, 
    'x_generate_complex_scenarios__mutmut_122': x_generate_complex_scenarios__mutmut_122, 
    'x_generate_complex_scenarios__mutmut_123': x_generate_complex_scenarios__mutmut_123, 
    'x_generate_complex_scenarios__mutmut_124': x_generate_complex_scenarios__mutmut_124, 
    'x_generate_complex_scenarios__mutmut_125': x_generate_complex_scenarios__mutmut_125, 
    'x_generate_complex_scenarios__mutmut_126': x_generate_complex_scenarios__mutmut_126, 
    'x_generate_complex_scenarios__mutmut_127': x_generate_complex_scenarios__mutmut_127, 
    'x_generate_complex_scenarios__mutmut_128': x_generate_complex_scenarios__mutmut_128, 
    'x_generate_complex_scenarios__mutmut_129': x_generate_complex_scenarios__mutmut_129, 
    'x_generate_complex_scenarios__mutmut_130': x_generate_complex_scenarios__mutmut_130, 
    'x_generate_complex_scenarios__mutmut_131': x_generate_complex_scenarios__mutmut_131, 
    'x_generate_complex_scenarios__mutmut_132': x_generate_complex_scenarios__mutmut_132, 
    'x_generate_complex_scenarios__mutmut_133': x_generate_complex_scenarios__mutmut_133, 
    'x_generate_complex_scenarios__mutmut_134': x_generate_complex_scenarios__mutmut_134, 
    'x_generate_complex_scenarios__mutmut_135': x_generate_complex_scenarios__mutmut_135, 
    'x_generate_complex_scenarios__mutmut_136': x_generate_complex_scenarios__mutmut_136, 
    'x_generate_complex_scenarios__mutmut_137': x_generate_complex_scenarios__mutmut_137, 
    'x_generate_complex_scenarios__mutmut_138': x_generate_complex_scenarios__mutmut_138, 
    'x_generate_complex_scenarios__mutmut_139': x_generate_complex_scenarios__mutmut_139, 
    'x_generate_complex_scenarios__mutmut_140': x_generate_complex_scenarios__mutmut_140, 
    'x_generate_complex_scenarios__mutmut_141': x_generate_complex_scenarios__mutmut_141, 
    'x_generate_complex_scenarios__mutmut_142': x_generate_complex_scenarios__mutmut_142, 
    'x_generate_complex_scenarios__mutmut_143': x_generate_complex_scenarios__mutmut_143, 
    'x_generate_complex_scenarios__mutmut_144': x_generate_complex_scenarios__mutmut_144, 
    'x_generate_complex_scenarios__mutmut_145': x_generate_complex_scenarios__mutmut_145, 
    'x_generate_complex_scenarios__mutmut_146': x_generate_complex_scenarios__mutmut_146, 
    'x_generate_complex_scenarios__mutmut_147': x_generate_complex_scenarios__mutmut_147, 
    'x_generate_complex_scenarios__mutmut_148': x_generate_complex_scenarios__mutmut_148, 
    'x_generate_complex_scenarios__mutmut_149': x_generate_complex_scenarios__mutmut_149, 
    'x_generate_complex_scenarios__mutmut_150': x_generate_complex_scenarios__mutmut_150, 
    'x_generate_complex_scenarios__mutmut_151': x_generate_complex_scenarios__mutmut_151, 
    'x_generate_complex_scenarios__mutmut_152': x_generate_complex_scenarios__mutmut_152, 
    'x_generate_complex_scenarios__mutmut_153': x_generate_complex_scenarios__mutmut_153, 
    'x_generate_complex_scenarios__mutmut_154': x_generate_complex_scenarios__mutmut_154, 
    'x_generate_complex_scenarios__mutmut_155': x_generate_complex_scenarios__mutmut_155, 
    'x_generate_complex_scenarios__mutmut_156': x_generate_complex_scenarios__mutmut_156, 
    'x_generate_complex_scenarios__mutmut_157': x_generate_complex_scenarios__mutmut_157, 
    'x_generate_complex_scenarios__mutmut_158': x_generate_complex_scenarios__mutmut_158, 
    'x_generate_complex_scenarios__mutmut_159': x_generate_complex_scenarios__mutmut_159, 
    'x_generate_complex_scenarios__mutmut_160': x_generate_complex_scenarios__mutmut_160, 
    'x_generate_complex_scenarios__mutmut_161': x_generate_complex_scenarios__mutmut_161, 
    'x_generate_complex_scenarios__mutmut_162': x_generate_complex_scenarios__mutmut_162, 
    'x_generate_complex_scenarios__mutmut_163': x_generate_complex_scenarios__mutmut_163, 
    'x_generate_complex_scenarios__mutmut_164': x_generate_complex_scenarios__mutmut_164, 
    'x_generate_complex_scenarios__mutmut_165': x_generate_complex_scenarios__mutmut_165, 
    'x_generate_complex_scenarios__mutmut_166': x_generate_complex_scenarios__mutmut_166, 
    'x_generate_complex_scenarios__mutmut_167': x_generate_complex_scenarios__mutmut_167, 
    'x_generate_complex_scenarios__mutmut_168': x_generate_complex_scenarios__mutmut_168, 
    'x_generate_complex_scenarios__mutmut_169': x_generate_complex_scenarios__mutmut_169, 
    'x_generate_complex_scenarios__mutmut_170': x_generate_complex_scenarios__mutmut_170, 
    'x_generate_complex_scenarios__mutmut_171': x_generate_complex_scenarios__mutmut_171, 
    'x_generate_complex_scenarios__mutmut_172': x_generate_complex_scenarios__mutmut_172, 
    'x_generate_complex_scenarios__mutmut_173': x_generate_complex_scenarios__mutmut_173, 
    'x_generate_complex_scenarios__mutmut_174': x_generate_complex_scenarios__mutmut_174, 
    'x_generate_complex_scenarios__mutmut_175': x_generate_complex_scenarios__mutmut_175, 
    'x_generate_complex_scenarios__mutmut_176': x_generate_complex_scenarios__mutmut_176, 
    'x_generate_complex_scenarios__mutmut_177': x_generate_complex_scenarios__mutmut_177, 
    'x_generate_complex_scenarios__mutmut_178': x_generate_complex_scenarios__mutmut_178, 
    'x_generate_complex_scenarios__mutmut_179': x_generate_complex_scenarios__mutmut_179, 
    'x_generate_complex_scenarios__mutmut_180': x_generate_complex_scenarios__mutmut_180, 
    'x_generate_complex_scenarios__mutmut_181': x_generate_complex_scenarios__mutmut_181, 
    'x_generate_complex_scenarios__mutmut_182': x_generate_complex_scenarios__mutmut_182, 
    'x_generate_complex_scenarios__mutmut_183': x_generate_complex_scenarios__mutmut_183, 
    'x_generate_complex_scenarios__mutmut_184': x_generate_complex_scenarios__mutmut_184, 
    'x_generate_complex_scenarios__mutmut_185': x_generate_complex_scenarios__mutmut_185, 
    'x_generate_complex_scenarios__mutmut_186': x_generate_complex_scenarios__mutmut_186, 
    'x_generate_complex_scenarios__mutmut_187': x_generate_complex_scenarios__mutmut_187, 
    'x_generate_complex_scenarios__mutmut_188': x_generate_complex_scenarios__mutmut_188, 
    'x_generate_complex_scenarios__mutmut_189': x_generate_complex_scenarios__mutmut_189, 
    'x_generate_complex_scenarios__mutmut_190': x_generate_complex_scenarios__mutmut_190, 
    'x_generate_complex_scenarios__mutmut_191': x_generate_complex_scenarios__mutmut_191, 
    'x_generate_complex_scenarios__mutmut_192': x_generate_complex_scenarios__mutmut_192, 
    'x_generate_complex_scenarios__mutmut_193': x_generate_complex_scenarios__mutmut_193, 
    'x_generate_complex_scenarios__mutmut_194': x_generate_complex_scenarios__mutmut_194, 
    'x_generate_complex_scenarios__mutmut_195': x_generate_complex_scenarios__mutmut_195, 
    'x_generate_complex_scenarios__mutmut_196': x_generate_complex_scenarios__mutmut_196, 
    'x_generate_complex_scenarios__mutmut_197': x_generate_complex_scenarios__mutmut_197, 
    'x_generate_complex_scenarios__mutmut_198': x_generate_complex_scenarios__mutmut_198, 
    'x_generate_complex_scenarios__mutmut_199': x_generate_complex_scenarios__mutmut_199, 
    'x_generate_complex_scenarios__mutmut_200': x_generate_complex_scenarios__mutmut_200, 
    'x_generate_complex_scenarios__mutmut_201': x_generate_complex_scenarios__mutmut_201, 
    'x_generate_complex_scenarios__mutmut_202': x_generate_complex_scenarios__mutmut_202, 
    'x_generate_complex_scenarios__mutmut_203': x_generate_complex_scenarios__mutmut_203, 
    'x_generate_complex_scenarios__mutmut_204': x_generate_complex_scenarios__mutmut_204, 
    'x_generate_complex_scenarios__mutmut_205': x_generate_complex_scenarios__mutmut_205, 
    'x_generate_complex_scenarios__mutmut_206': x_generate_complex_scenarios__mutmut_206, 
    'x_generate_complex_scenarios__mutmut_207': x_generate_complex_scenarios__mutmut_207, 
    'x_generate_complex_scenarios__mutmut_208': x_generate_complex_scenarios__mutmut_208, 
    'x_generate_complex_scenarios__mutmut_209': x_generate_complex_scenarios__mutmut_209, 
    'x_generate_complex_scenarios__mutmut_210': x_generate_complex_scenarios__mutmut_210, 
    'x_generate_complex_scenarios__mutmut_211': x_generate_complex_scenarios__mutmut_211, 
    'x_generate_complex_scenarios__mutmut_212': x_generate_complex_scenarios__mutmut_212, 
    'x_generate_complex_scenarios__mutmut_213': x_generate_complex_scenarios__mutmut_213, 
    'x_generate_complex_scenarios__mutmut_214': x_generate_complex_scenarios__mutmut_214, 
    'x_generate_complex_scenarios__mutmut_215': x_generate_complex_scenarios__mutmut_215, 
    'x_generate_complex_scenarios__mutmut_216': x_generate_complex_scenarios__mutmut_216, 
    'x_generate_complex_scenarios__mutmut_217': x_generate_complex_scenarios__mutmut_217, 
    'x_generate_complex_scenarios__mutmut_218': x_generate_complex_scenarios__mutmut_218, 
    'x_generate_complex_scenarios__mutmut_219': x_generate_complex_scenarios__mutmut_219, 
    'x_generate_complex_scenarios__mutmut_220': x_generate_complex_scenarios__mutmut_220, 
    'x_generate_complex_scenarios__mutmut_221': x_generate_complex_scenarios__mutmut_221, 
    'x_generate_complex_scenarios__mutmut_222': x_generate_complex_scenarios__mutmut_222, 
    'x_generate_complex_scenarios__mutmut_223': x_generate_complex_scenarios__mutmut_223, 
    'x_generate_complex_scenarios__mutmut_224': x_generate_complex_scenarios__mutmut_224, 
    'x_generate_complex_scenarios__mutmut_225': x_generate_complex_scenarios__mutmut_225, 
    'x_generate_complex_scenarios__mutmut_226': x_generate_complex_scenarios__mutmut_226, 
    'x_generate_complex_scenarios__mutmut_227': x_generate_complex_scenarios__mutmut_227, 
    'x_generate_complex_scenarios__mutmut_228': x_generate_complex_scenarios__mutmut_228, 
    'x_generate_complex_scenarios__mutmut_229': x_generate_complex_scenarios__mutmut_229, 
    'x_generate_complex_scenarios__mutmut_230': x_generate_complex_scenarios__mutmut_230, 
    'x_generate_complex_scenarios__mutmut_231': x_generate_complex_scenarios__mutmut_231, 
    'x_generate_complex_scenarios__mutmut_232': x_generate_complex_scenarios__mutmut_232, 
    'x_generate_complex_scenarios__mutmut_233': x_generate_complex_scenarios__mutmut_233, 
    'x_generate_complex_scenarios__mutmut_234': x_generate_complex_scenarios__mutmut_234, 
    'x_generate_complex_scenarios__mutmut_235': x_generate_complex_scenarios__mutmut_235, 
    'x_generate_complex_scenarios__mutmut_236': x_generate_complex_scenarios__mutmut_236, 
    'x_generate_complex_scenarios__mutmut_237': x_generate_complex_scenarios__mutmut_237, 
    'x_generate_complex_scenarios__mutmut_238': x_generate_complex_scenarios__mutmut_238, 
    'x_generate_complex_scenarios__mutmut_239': x_generate_complex_scenarios__mutmut_239, 
    'x_generate_complex_scenarios__mutmut_240': x_generate_complex_scenarios__mutmut_240, 
    'x_generate_complex_scenarios__mutmut_241': x_generate_complex_scenarios__mutmut_241, 
    'x_generate_complex_scenarios__mutmut_242': x_generate_complex_scenarios__mutmut_242, 
    'x_generate_complex_scenarios__mutmut_243': x_generate_complex_scenarios__mutmut_243, 
    'x_generate_complex_scenarios__mutmut_244': x_generate_complex_scenarios__mutmut_244, 
    'x_generate_complex_scenarios__mutmut_245': x_generate_complex_scenarios__mutmut_245, 
    'x_generate_complex_scenarios__mutmut_246': x_generate_complex_scenarios__mutmut_246, 
    'x_generate_complex_scenarios__mutmut_247': x_generate_complex_scenarios__mutmut_247, 
    'x_generate_complex_scenarios__mutmut_248': x_generate_complex_scenarios__mutmut_248, 
    'x_generate_complex_scenarios__mutmut_249': x_generate_complex_scenarios__mutmut_249, 
    'x_generate_complex_scenarios__mutmut_250': x_generate_complex_scenarios__mutmut_250, 
    'x_generate_complex_scenarios__mutmut_251': x_generate_complex_scenarios__mutmut_251, 
    'x_generate_complex_scenarios__mutmut_252': x_generate_complex_scenarios__mutmut_252, 
    'x_generate_complex_scenarios__mutmut_253': x_generate_complex_scenarios__mutmut_253, 
    'x_generate_complex_scenarios__mutmut_254': x_generate_complex_scenarios__mutmut_254, 
    'x_generate_complex_scenarios__mutmut_255': x_generate_complex_scenarios__mutmut_255, 
    'x_generate_complex_scenarios__mutmut_256': x_generate_complex_scenarios__mutmut_256, 
    'x_generate_complex_scenarios__mutmut_257': x_generate_complex_scenarios__mutmut_257, 
    'x_generate_complex_scenarios__mutmut_258': x_generate_complex_scenarios__mutmut_258, 
    'x_generate_complex_scenarios__mutmut_259': x_generate_complex_scenarios__mutmut_259, 
    'x_generate_complex_scenarios__mutmut_260': x_generate_complex_scenarios__mutmut_260, 
    'x_generate_complex_scenarios__mutmut_261': x_generate_complex_scenarios__mutmut_261, 
    'x_generate_complex_scenarios__mutmut_262': x_generate_complex_scenarios__mutmut_262, 
    'x_generate_complex_scenarios__mutmut_263': x_generate_complex_scenarios__mutmut_263, 
    'x_generate_complex_scenarios__mutmut_264': x_generate_complex_scenarios__mutmut_264, 
    'x_generate_complex_scenarios__mutmut_265': x_generate_complex_scenarios__mutmut_265, 
    'x_generate_complex_scenarios__mutmut_266': x_generate_complex_scenarios__mutmut_266, 
    'x_generate_complex_scenarios__mutmut_267': x_generate_complex_scenarios__mutmut_267, 
    'x_generate_complex_scenarios__mutmut_268': x_generate_complex_scenarios__mutmut_268, 
    'x_generate_complex_scenarios__mutmut_269': x_generate_complex_scenarios__mutmut_269, 
    'x_generate_complex_scenarios__mutmut_270': x_generate_complex_scenarios__mutmut_270, 
    'x_generate_complex_scenarios__mutmut_271': x_generate_complex_scenarios__mutmut_271, 
    'x_generate_complex_scenarios__mutmut_272': x_generate_complex_scenarios__mutmut_272, 
    'x_generate_complex_scenarios__mutmut_273': x_generate_complex_scenarios__mutmut_273, 
    'x_generate_complex_scenarios__mutmut_274': x_generate_complex_scenarios__mutmut_274, 
    'x_generate_complex_scenarios__mutmut_275': x_generate_complex_scenarios__mutmut_275, 
    'x_generate_complex_scenarios__mutmut_276': x_generate_complex_scenarios__mutmut_276, 
    'x_generate_complex_scenarios__mutmut_277': x_generate_complex_scenarios__mutmut_277, 
    'x_generate_complex_scenarios__mutmut_278': x_generate_complex_scenarios__mutmut_278, 
    'x_generate_complex_scenarios__mutmut_279': x_generate_complex_scenarios__mutmut_279, 
    'x_generate_complex_scenarios__mutmut_280': x_generate_complex_scenarios__mutmut_280, 
    'x_generate_complex_scenarios__mutmut_281': x_generate_complex_scenarios__mutmut_281, 
    'x_generate_complex_scenarios__mutmut_282': x_generate_complex_scenarios__mutmut_282, 
    'x_generate_complex_scenarios__mutmut_283': x_generate_complex_scenarios__mutmut_283, 
    'x_generate_complex_scenarios__mutmut_284': x_generate_complex_scenarios__mutmut_284, 
    'x_generate_complex_scenarios__mutmut_285': x_generate_complex_scenarios__mutmut_285, 
    'x_generate_complex_scenarios__mutmut_286': x_generate_complex_scenarios__mutmut_286, 
    'x_generate_complex_scenarios__mutmut_287': x_generate_complex_scenarios__mutmut_287, 
    'x_generate_complex_scenarios__mutmut_288': x_generate_complex_scenarios__mutmut_288, 
    'x_generate_complex_scenarios__mutmut_289': x_generate_complex_scenarios__mutmut_289, 
    'x_generate_complex_scenarios__mutmut_290': x_generate_complex_scenarios__mutmut_290, 
    'x_generate_complex_scenarios__mutmut_291': x_generate_complex_scenarios__mutmut_291, 
    'x_generate_complex_scenarios__mutmut_292': x_generate_complex_scenarios__mutmut_292, 
    'x_generate_complex_scenarios__mutmut_293': x_generate_complex_scenarios__mutmut_293, 
    'x_generate_complex_scenarios__mutmut_294': x_generate_complex_scenarios__mutmut_294, 
    'x_generate_complex_scenarios__mutmut_295': x_generate_complex_scenarios__mutmut_295, 
    'x_generate_complex_scenarios__mutmut_296': x_generate_complex_scenarios__mutmut_296, 
    'x_generate_complex_scenarios__mutmut_297': x_generate_complex_scenarios__mutmut_297, 
    'x_generate_complex_scenarios__mutmut_298': x_generate_complex_scenarios__mutmut_298, 
    'x_generate_complex_scenarios__mutmut_299': x_generate_complex_scenarios__mutmut_299, 
    'x_generate_complex_scenarios__mutmut_300': x_generate_complex_scenarios__mutmut_300, 
    'x_generate_complex_scenarios__mutmut_301': x_generate_complex_scenarios__mutmut_301, 
    'x_generate_complex_scenarios__mutmut_302': x_generate_complex_scenarios__mutmut_302, 
    'x_generate_complex_scenarios__mutmut_303': x_generate_complex_scenarios__mutmut_303, 
    'x_generate_complex_scenarios__mutmut_304': x_generate_complex_scenarios__mutmut_304, 
    'x_generate_complex_scenarios__mutmut_305': x_generate_complex_scenarios__mutmut_305, 
    'x_generate_complex_scenarios__mutmut_306': x_generate_complex_scenarios__mutmut_306, 
    'x_generate_complex_scenarios__mutmut_307': x_generate_complex_scenarios__mutmut_307, 
    'x_generate_complex_scenarios__mutmut_308': x_generate_complex_scenarios__mutmut_308, 
    'x_generate_complex_scenarios__mutmut_309': x_generate_complex_scenarios__mutmut_309, 
    'x_generate_complex_scenarios__mutmut_310': x_generate_complex_scenarios__mutmut_310, 
    'x_generate_complex_scenarios__mutmut_311': x_generate_complex_scenarios__mutmut_311, 
    'x_generate_complex_scenarios__mutmut_312': x_generate_complex_scenarios__mutmut_312, 
    'x_generate_complex_scenarios__mutmut_313': x_generate_complex_scenarios__mutmut_313, 
    'x_generate_complex_scenarios__mutmut_314': x_generate_complex_scenarios__mutmut_314, 
    'x_generate_complex_scenarios__mutmut_315': x_generate_complex_scenarios__mutmut_315, 
    'x_generate_complex_scenarios__mutmut_316': x_generate_complex_scenarios__mutmut_316, 
    'x_generate_complex_scenarios__mutmut_317': x_generate_complex_scenarios__mutmut_317, 
    'x_generate_complex_scenarios__mutmut_318': x_generate_complex_scenarios__mutmut_318, 
    'x_generate_complex_scenarios__mutmut_319': x_generate_complex_scenarios__mutmut_319, 
    'x_generate_complex_scenarios__mutmut_320': x_generate_complex_scenarios__mutmut_320, 
    'x_generate_complex_scenarios__mutmut_321': x_generate_complex_scenarios__mutmut_321, 
    'x_generate_complex_scenarios__mutmut_322': x_generate_complex_scenarios__mutmut_322, 
    'x_generate_complex_scenarios__mutmut_323': x_generate_complex_scenarios__mutmut_323, 
    'x_generate_complex_scenarios__mutmut_324': x_generate_complex_scenarios__mutmut_324, 
    'x_generate_complex_scenarios__mutmut_325': x_generate_complex_scenarios__mutmut_325, 
    'x_generate_complex_scenarios__mutmut_326': x_generate_complex_scenarios__mutmut_326, 
    'x_generate_complex_scenarios__mutmut_327': x_generate_complex_scenarios__mutmut_327, 
    'x_generate_complex_scenarios__mutmut_328': x_generate_complex_scenarios__mutmut_328, 
    'x_generate_complex_scenarios__mutmut_329': x_generate_complex_scenarios__mutmut_329, 
    'x_generate_complex_scenarios__mutmut_330': x_generate_complex_scenarios__mutmut_330, 
    'x_generate_complex_scenarios__mutmut_331': x_generate_complex_scenarios__mutmut_331, 
    'x_generate_complex_scenarios__mutmut_332': x_generate_complex_scenarios__mutmut_332, 
    'x_generate_complex_scenarios__mutmut_333': x_generate_complex_scenarios__mutmut_333, 
    'x_generate_complex_scenarios__mutmut_334': x_generate_complex_scenarios__mutmut_334, 
    'x_generate_complex_scenarios__mutmut_335': x_generate_complex_scenarios__mutmut_335, 
    'x_generate_complex_scenarios__mutmut_336': x_generate_complex_scenarios__mutmut_336, 
    'x_generate_complex_scenarios__mutmut_337': x_generate_complex_scenarios__mutmut_337, 
    'x_generate_complex_scenarios__mutmut_338': x_generate_complex_scenarios__mutmut_338, 
    'x_generate_complex_scenarios__mutmut_339': x_generate_complex_scenarios__mutmut_339, 
    'x_generate_complex_scenarios__mutmut_340': x_generate_complex_scenarios__mutmut_340, 
    'x_generate_complex_scenarios__mutmut_341': x_generate_complex_scenarios__mutmut_341, 
    'x_generate_complex_scenarios__mutmut_342': x_generate_complex_scenarios__mutmut_342, 
    'x_generate_complex_scenarios__mutmut_343': x_generate_complex_scenarios__mutmut_343, 
    'x_generate_complex_scenarios__mutmut_344': x_generate_complex_scenarios__mutmut_344, 
    'x_generate_complex_scenarios__mutmut_345': x_generate_complex_scenarios__mutmut_345, 
    'x_generate_complex_scenarios__mutmut_346': x_generate_complex_scenarios__mutmut_346, 
    'x_generate_complex_scenarios__mutmut_347': x_generate_complex_scenarios__mutmut_347, 
    'x_generate_complex_scenarios__mutmut_348': x_generate_complex_scenarios__mutmut_348, 
    'x_generate_complex_scenarios__mutmut_349': x_generate_complex_scenarios__mutmut_349, 
    'x_generate_complex_scenarios__mutmut_350': x_generate_complex_scenarios__mutmut_350, 
    'x_generate_complex_scenarios__mutmut_351': x_generate_complex_scenarios__mutmut_351, 
    'x_generate_complex_scenarios__mutmut_352': x_generate_complex_scenarios__mutmut_352, 
    'x_generate_complex_scenarios__mutmut_353': x_generate_complex_scenarios__mutmut_353, 
    'x_generate_complex_scenarios__mutmut_354': x_generate_complex_scenarios__mutmut_354, 
    'x_generate_complex_scenarios__mutmut_355': x_generate_complex_scenarios__mutmut_355, 
    'x_generate_complex_scenarios__mutmut_356': x_generate_complex_scenarios__mutmut_356, 
    'x_generate_complex_scenarios__mutmut_357': x_generate_complex_scenarios__mutmut_357, 
    'x_generate_complex_scenarios__mutmut_358': x_generate_complex_scenarios__mutmut_358, 
    'x_generate_complex_scenarios__mutmut_359': x_generate_complex_scenarios__mutmut_359, 
    'x_generate_complex_scenarios__mutmut_360': x_generate_complex_scenarios__mutmut_360, 
    'x_generate_complex_scenarios__mutmut_361': x_generate_complex_scenarios__mutmut_361, 
    'x_generate_complex_scenarios__mutmut_362': x_generate_complex_scenarios__mutmut_362, 
    'x_generate_complex_scenarios__mutmut_363': x_generate_complex_scenarios__mutmut_363, 
    'x_generate_complex_scenarios__mutmut_364': x_generate_complex_scenarios__mutmut_364, 
    'x_generate_complex_scenarios__mutmut_365': x_generate_complex_scenarios__mutmut_365, 
    'x_generate_complex_scenarios__mutmut_366': x_generate_complex_scenarios__mutmut_366, 
    'x_generate_complex_scenarios__mutmut_367': x_generate_complex_scenarios__mutmut_367, 
    'x_generate_complex_scenarios__mutmut_368': x_generate_complex_scenarios__mutmut_368, 
    'x_generate_complex_scenarios__mutmut_369': x_generate_complex_scenarios__mutmut_369, 
    'x_generate_complex_scenarios__mutmut_370': x_generate_complex_scenarios__mutmut_370, 
    'x_generate_complex_scenarios__mutmut_371': x_generate_complex_scenarios__mutmut_371, 
    'x_generate_complex_scenarios__mutmut_372': x_generate_complex_scenarios__mutmut_372, 
    'x_generate_complex_scenarios__mutmut_373': x_generate_complex_scenarios__mutmut_373, 
    'x_generate_complex_scenarios__mutmut_374': x_generate_complex_scenarios__mutmut_374, 
    'x_generate_complex_scenarios__mutmut_375': x_generate_complex_scenarios__mutmut_375, 
    'x_generate_complex_scenarios__mutmut_376': x_generate_complex_scenarios__mutmut_376, 
    'x_generate_complex_scenarios__mutmut_377': x_generate_complex_scenarios__mutmut_377, 
    'x_generate_complex_scenarios__mutmut_378': x_generate_complex_scenarios__mutmut_378, 
    'x_generate_complex_scenarios__mutmut_379': x_generate_complex_scenarios__mutmut_379, 
    'x_generate_complex_scenarios__mutmut_380': x_generate_complex_scenarios__mutmut_380, 
    'x_generate_complex_scenarios__mutmut_381': x_generate_complex_scenarios__mutmut_381, 
    'x_generate_complex_scenarios__mutmut_382': x_generate_complex_scenarios__mutmut_382, 
    'x_generate_complex_scenarios__mutmut_383': x_generate_complex_scenarios__mutmut_383, 
    'x_generate_complex_scenarios__mutmut_384': x_generate_complex_scenarios__mutmut_384, 
    'x_generate_complex_scenarios__mutmut_385': x_generate_complex_scenarios__mutmut_385, 
    'x_generate_complex_scenarios__mutmut_386': x_generate_complex_scenarios__mutmut_386, 
    'x_generate_complex_scenarios__mutmut_387': x_generate_complex_scenarios__mutmut_387, 
    'x_generate_complex_scenarios__mutmut_388': x_generate_complex_scenarios__mutmut_388, 
    'x_generate_complex_scenarios__mutmut_389': x_generate_complex_scenarios__mutmut_389, 
    'x_generate_complex_scenarios__mutmut_390': x_generate_complex_scenarios__mutmut_390, 
    'x_generate_complex_scenarios__mutmut_391': x_generate_complex_scenarios__mutmut_391, 
    'x_generate_complex_scenarios__mutmut_392': x_generate_complex_scenarios__mutmut_392, 
    'x_generate_complex_scenarios__mutmut_393': x_generate_complex_scenarios__mutmut_393, 
    'x_generate_complex_scenarios__mutmut_394': x_generate_complex_scenarios__mutmut_394, 
    'x_generate_complex_scenarios__mutmut_395': x_generate_complex_scenarios__mutmut_395, 
    'x_generate_complex_scenarios__mutmut_396': x_generate_complex_scenarios__mutmut_396, 
    'x_generate_complex_scenarios__mutmut_397': x_generate_complex_scenarios__mutmut_397, 
    'x_generate_complex_scenarios__mutmut_398': x_generate_complex_scenarios__mutmut_398, 
    'x_generate_complex_scenarios__mutmut_399': x_generate_complex_scenarios__mutmut_399, 
    'x_generate_complex_scenarios__mutmut_400': x_generate_complex_scenarios__mutmut_400, 
    'x_generate_complex_scenarios__mutmut_401': x_generate_complex_scenarios__mutmut_401, 
    'x_generate_complex_scenarios__mutmut_402': x_generate_complex_scenarios__mutmut_402, 
    'x_generate_complex_scenarios__mutmut_403': x_generate_complex_scenarios__mutmut_403, 
    'x_generate_complex_scenarios__mutmut_404': x_generate_complex_scenarios__mutmut_404, 
    'x_generate_complex_scenarios__mutmut_405': x_generate_complex_scenarios__mutmut_405, 
    'x_generate_complex_scenarios__mutmut_406': x_generate_complex_scenarios__mutmut_406, 
    'x_generate_complex_scenarios__mutmut_407': x_generate_complex_scenarios__mutmut_407, 
    'x_generate_complex_scenarios__mutmut_408': x_generate_complex_scenarios__mutmut_408, 
    'x_generate_complex_scenarios__mutmut_409': x_generate_complex_scenarios__mutmut_409, 
    'x_generate_complex_scenarios__mutmut_410': x_generate_complex_scenarios__mutmut_410, 
    'x_generate_complex_scenarios__mutmut_411': x_generate_complex_scenarios__mutmut_411, 
    'x_generate_complex_scenarios__mutmut_412': x_generate_complex_scenarios__mutmut_412, 
    'x_generate_complex_scenarios__mutmut_413': x_generate_complex_scenarios__mutmut_413, 
    'x_generate_complex_scenarios__mutmut_414': x_generate_complex_scenarios__mutmut_414, 
    'x_generate_complex_scenarios__mutmut_415': x_generate_complex_scenarios__mutmut_415, 
    'x_generate_complex_scenarios__mutmut_416': x_generate_complex_scenarios__mutmut_416, 
    'x_generate_complex_scenarios__mutmut_417': x_generate_complex_scenarios__mutmut_417, 
    'x_generate_complex_scenarios__mutmut_418': x_generate_complex_scenarios__mutmut_418, 
    'x_generate_complex_scenarios__mutmut_419': x_generate_complex_scenarios__mutmut_419, 
    'x_generate_complex_scenarios__mutmut_420': x_generate_complex_scenarios__mutmut_420, 
    'x_generate_complex_scenarios__mutmut_421': x_generate_complex_scenarios__mutmut_421, 
    'x_generate_complex_scenarios__mutmut_422': x_generate_complex_scenarios__mutmut_422, 
    'x_generate_complex_scenarios__mutmut_423': x_generate_complex_scenarios__mutmut_423, 
    'x_generate_complex_scenarios__mutmut_424': x_generate_complex_scenarios__mutmut_424, 
    'x_generate_complex_scenarios__mutmut_425': x_generate_complex_scenarios__mutmut_425, 
    'x_generate_complex_scenarios__mutmut_426': x_generate_complex_scenarios__mutmut_426, 
    'x_generate_complex_scenarios__mutmut_427': x_generate_complex_scenarios__mutmut_427, 
    'x_generate_complex_scenarios__mutmut_428': x_generate_complex_scenarios__mutmut_428, 
    'x_generate_complex_scenarios__mutmut_429': x_generate_complex_scenarios__mutmut_429, 
    'x_generate_complex_scenarios__mutmut_430': x_generate_complex_scenarios__mutmut_430, 
    'x_generate_complex_scenarios__mutmut_431': x_generate_complex_scenarios__mutmut_431, 
    'x_generate_complex_scenarios__mutmut_432': x_generate_complex_scenarios__mutmut_432, 
    'x_generate_complex_scenarios__mutmut_433': x_generate_complex_scenarios__mutmut_433, 
    'x_generate_complex_scenarios__mutmut_434': x_generate_complex_scenarios__mutmut_434, 
    'x_generate_complex_scenarios__mutmut_435': x_generate_complex_scenarios__mutmut_435, 
    'x_generate_complex_scenarios__mutmut_436': x_generate_complex_scenarios__mutmut_436, 
    'x_generate_complex_scenarios__mutmut_437': x_generate_complex_scenarios__mutmut_437, 
    'x_generate_complex_scenarios__mutmut_438': x_generate_complex_scenarios__mutmut_438, 
    'x_generate_complex_scenarios__mutmut_439': x_generate_complex_scenarios__mutmut_439, 
    'x_generate_complex_scenarios__mutmut_440': x_generate_complex_scenarios__mutmut_440, 
    'x_generate_complex_scenarios__mutmut_441': x_generate_complex_scenarios__mutmut_441, 
    'x_generate_complex_scenarios__mutmut_442': x_generate_complex_scenarios__mutmut_442, 
    'x_generate_complex_scenarios__mutmut_443': x_generate_complex_scenarios__mutmut_443, 
    'x_generate_complex_scenarios__mutmut_444': x_generate_complex_scenarios__mutmut_444, 
    'x_generate_complex_scenarios__mutmut_445': x_generate_complex_scenarios__mutmut_445, 
    'x_generate_complex_scenarios__mutmut_446': x_generate_complex_scenarios__mutmut_446, 
    'x_generate_complex_scenarios__mutmut_447': x_generate_complex_scenarios__mutmut_447, 
    'x_generate_complex_scenarios__mutmut_448': x_generate_complex_scenarios__mutmut_448, 
    'x_generate_complex_scenarios__mutmut_449': x_generate_complex_scenarios__mutmut_449, 
    'x_generate_complex_scenarios__mutmut_450': x_generate_complex_scenarios__mutmut_450, 
    'x_generate_complex_scenarios__mutmut_451': x_generate_complex_scenarios__mutmut_451, 
    'x_generate_complex_scenarios__mutmut_452': x_generate_complex_scenarios__mutmut_452, 
    'x_generate_complex_scenarios__mutmut_453': x_generate_complex_scenarios__mutmut_453, 
    'x_generate_complex_scenarios__mutmut_454': x_generate_complex_scenarios__mutmut_454, 
    'x_generate_complex_scenarios__mutmut_455': x_generate_complex_scenarios__mutmut_455, 
    'x_generate_complex_scenarios__mutmut_456': x_generate_complex_scenarios__mutmut_456, 
    'x_generate_complex_scenarios__mutmut_457': x_generate_complex_scenarios__mutmut_457, 
    'x_generate_complex_scenarios__mutmut_458': x_generate_complex_scenarios__mutmut_458, 
    'x_generate_complex_scenarios__mutmut_459': x_generate_complex_scenarios__mutmut_459, 
    'x_generate_complex_scenarios__mutmut_460': x_generate_complex_scenarios__mutmut_460, 
    'x_generate_complex_scenarios__mutmut_461': x_generate_complex_scenarios__mutmut_461, 
    'x_generate_complex_scenarios__mutmut_462': x_generate_complex_scenarios__mutmut_462, 
    'x_generate_complex_scenarios__mutmut_463': x_generate_complex_scenarios__mutmut_463, 
    'x_generate_complex_scenarios__mutmut_464': x_generate_complex_scenarios__mutmut_464, 
    'x_generate_complex_scenarios__mutmut_465': x_generate_complex_scenarios__mutmut_465, 
    'x_generate_complex_scenarios__mutmut_466': x_generate_complex_scenarios__mutmut_466, 
    'x_generate_complex_scenarios__mutmut_467': x_generate_complex_scenarios__mutmut_467, 
    'x_generate_complex_scenarios__mutmut_468': x_generate_complex_scenarios__mutmut_468, 
    'x_generate_complex_scenarios__mutmut_469': x_generate_complex_scenarios__mutmut_469, 
    'x_generate_complex_scenarios__mutmut_470': x_generate_complex_scenarios__mutmut_470, 
    'x_generate_complex_scenarios__mutmut_471': x_generate_complex_scenarios__mutmut_471, 
    'x_generate_complex_scenarios__mutmut_472': x_generate_complex_scenarios__mutmut_472, 
    'x_generate_complex_scenarios__mutmut_473': x_generate_complex_scenarios__mutmut_473, 
    'x_generate_complex_scenarios__mutmut_474': x_generate_complex_scenarios__mutmut_474, 
    'x_generate_complex_scenarios__mutmut_475': x_generate_complex_scenarios__mutmut_475, 
    'x_generate_complex_scenarios__mutmut_476': x_generate_complex_scenarios__mutmut_476, 
    'x_generate_complex_scenarios__mutmut_477': x_generate_complex_scenarios__mutmut_477, 
    'x_generate_complex_scenarios__mutmut_478': x_generate_complex_scenarios__mutmut_478, 
    'x_generate_complex_scenarios__mutmut_479': x_generate_complex_scenarios__mutmut_479, 
    'x_generate_complex_scenarios__mutmut_480': x_generate_complex_scenarios__mutmut_480, 
    'x_generate_complex_scenarios__mutmut_481': x_generate_complex_scenarios__mutmut_481, 
    'x_generate_complex_scenarios__mutmut_482': x_generate_complex_scenarios__mutmut_482, 
    'x_generate_complex_scenarios__mutmut_483': x_generate_complex_scenarios__mutmut_483, 
    'x_generate_complex_scenarios__mutmut_484': x_generate_complex_scenarios__mutmut_484, 
    'x_generate_complex_scenarios__mutmut_485': x_generate_complex_scenarios__mutmut_485, 
    'x_generate_complex_scenarios__mutmut_486': x_generate_complex_scenarios__mutmut_486, 
    'x_generate_complex_scenarios__mutmut_487': x_generate_complex_scenarios__mutmut_487, 
    'x_generate_complex_scenarios__mutmut_488': x_generate_complex_scenarios__mutmut_488, 
    'x_generate_complex_scenarios__mutmut_489': x_generate_complex_scenarios__mutmut_489, 
    'x_generate_complex_scenarios__mutmut_490': x_generate_complex_scenarios__mutmut_490, 
    'x_generate_complex_scenarios__mutmut_491': x_generate_complex_scenarios__mutmut_491, 
    'x_generate_complex_scenarios__mutmut_492': x_generate_complex_scenarios__mutmut_492, 
    'x_generate_complex_scenarios__mutmut_493': x_generate_complex_scenarios__mutmut_493, 
    'x_generate_complex_scenarios__mutmut_494': x_generate_complex_scenarios__mutmut_494, 
    'x_generate_complex_scenarios__mutmut_495': x_generate_complex_scenarios__mutmut_495, 
    'x_generate_complex_scenarios__mutmut_496': x_generate_complex_scenarios__mutmut_496, 
    'x_generate_complex_scenarios__mutmut_497': x_generate_complex_scenarios__mutmut_497, 
    'x_generate_complex_scenarios__mutmut_498': x_generate_complex_scenarios__mutmut_498, 
    'x_generate_complex_scenarios__mutmut_499': x_generate_complex_scenarios__mutmut_499, 
    'x_generate_complex_scenarios__mutmut_500': x_generate_complex_scenarios__mutmut_500, 
    'x_generate_complex_scenarios__mutmut_501': x_generate_complex_scenarios__mutmut_501, 
    'x_generate_complex_scenarios__mutmut_502': x_generate_complex_scenarios__mutmut_502, 
    'x_generate_complex_scenarios__mutmut_503': x_generate_complex_scenarios__mutmut_503, 
    'x_generate_complex_scenarios__mutmut_504': x_generate_complex_scenarios__mutmut_504, 
    'x_generate_complex_scenarios__mutmut_505': x_generate_complex_scenarios__mutmut_505, 
    'x_generate_complex_scenarios__mutmut_506': x_generate_complex_scenarios__mutmut_506, 
    'x_generate_complex_scenarios__mutmut_507': x_generate_complex_scenarios__mutmut_507, 
    'x_generate_complex_scenarios__mutmut_508': x_generate_complex_scenarios__mutmut_508, 
    'x_generate_complex_scenarios__mutmut_509': x_generate_complex_scenarios__mutmut_509, 
    'x_generate_complex_scenarios__mutmut_510': x_generate_complex_scenarios__mutmut_510, 
    'x_generate_complex_scenarios__mutmut_511': x_generate_complex_scenarios__mutmut_511, 
    'x_generate_complex_scenarios__mutmut_512': x_generate_complex_scenarios__mutmut_512, 
    'x_generate_complex_scenarios__mutmut_513': x_generate_complex_scenarios__mutmut_513, 
    'x_generate_complex_scenarios__mutmut_514': x_generate_complex_scenarios__mutmut_514, 
    'x_generate_complex_scenarios__mutmut_515': x_generate_complex_scenarios__mutmut_515, 
    'x_generate_complex_scenarios__mutmut_516': x_generate_complex_scenarios__mutmut_516, 
    'x_generate_complex_scenarios__mutmut_517': x_generate_complex_scenarios__mutmut_517, 
    'x_generate_complex_scenarios__mutmut_518': x_generate_complex_scenarios__mutmut_518, 
    'x_generate_complex_scenarios__mutmut_519': x_generate_complex_scenarios__mutmut_519, 
    'x_generate_complex_scenarios__mutmut_520': x_generate_complex_scenarios__mutmut_520, 
    'x_generate_complex_scenarios__mutmut_521': x_generate_complex_scenarios__mutmut_521, 
    'x_generate_complex_scenarios__mutmut_522': x_generate_complex_scenarios__mutmut_522, 
    'x_generate_complex_scenarios__mutmut_523': x_generate_complex_scenarios__mutmut_523, 
    'x_generate_complex_scenarios__mutmut_524': x_generate_complex_scenarios__mutmut_524, 
    'x_generate_complex_scenarios__mutmut_525': x_generate_complex_scenarios__mutmut_525, 
    'x_generate_complex_scenarios__mutmut_526': x_generate_complex_scenarios__mutmut_526, 
    'x_generate_complex_scenarios__mutmut_527': x_generate_complex_scenarios__mutmut_527, 
    'x_generate_complex_scenarios__mutmut_528': x_generate_complex_scenarios__mutmut_528, 
    'x_generate_complex_scenarios__mutmut_529': x_generate_complex_scenarios__mutmut_529, 
    'x_generate_complex_scenarios__mutmut_530': x_generate_complex_scenarios__mutmut_530, 
    'x_generate_complex_scenarios__mutmut_531': x_generate_complex_scenarios__mutmut_531, 
    'x_generate_complex_scenarios__mutmut_532': x_generate_complex_scenarios__mutmut_532, 
    'x_generate_complex_scenarios__mutmut_533': x_generate_complex_scenarios__mutmut_533, 
    'x_generate_complex_scenarios__mutmut_534': x_generate_complex_scenarios__mutmut_534, 
    'x_generate_complex_scenarios__mutmut_535': x_generate_complex_scenarios__mutmut_535, 
    'x_generate_complex_scenarios__mutmut_536': x_generate_complex_scenarios__mutmut_536, 
    'x_generate_complex_scenarios__mutmut_537': x_generate_complex_scenarios__mutmut_537, 
    'x_generate_complex_scenarios__mutmut_538': x_generate_complex_scenarios__mutmut_538, 
    'x_generate_complex_scenarios__mutmut_539': x_generate_complex_scenarios__mutmut_539, 
    'x_generate_complex_scenarios__mutmut_540': x_generate_complex_scenarios__mutmut_540, 
    'x_generate_complex_scenarios__mutmut_541': x_generate_complex_scenarios__mutmut_541, 
    'x_generate_complex_scenarios__mutmut_542': x_generate_complex_scenarios__mutmut_542, 
    'x_generate_complex_scenarios__mutmut_543': x_generate_complex_scenarios__mutmut_543, 
    'x_generate_complex_scenarios__mutmut_544': x_generate_complex_scenarios__mutmut_544, 
    'x_generate_complex_scenarios__mutmut_545': x_generate_complex_scenarios__mutmut_545, 
    'x_generate_complex_scenarios__mutmut_546': x_generate_complex_scenarios__mutmut_546, 
    'x_generate_complex_scenarios__mutmut_547': x_generate_complex_scenarios__mutmut_547, 
    'x_generate_complex_scenarios__mutmut_548': x_generate_complex_scenarios__mutmut_548, 
    'x_generate_complex_scenarios__mutmut_549': x_generate_complex_scenarios__mutmut_549, 
    'x_generate_complex_scenarios__mutmut_550': x_generate_complex_scenarios__mutmut_550, 
    'x_generate_complex_scenarios__mutmut_551': x_generate_complex_scenarios__mutmut_551, 
    'x_generate_complex_scenarios__mutmut_552': x_generate_complex_scenarios__mutmut_552, 
    'x_generate_complex_scenarios__mutmut_553': x_generate_complex_scenarios__mutmut_553, 
    'x_generate_complex_scenarios__mutmut_554': x_generate_complex_scenarios__mutmut_554, 
    'x_generate_complex_scenarios__mutmut_555': x_generate_complex_scenarios__mutmut_555, 
    'x_generate_complex_scenarios__mutmut_556': x_generate_complex_scenarios__mutmut_556, 
    'x_generate_complex_scenarios__mutmut_557': x_generate_complex_scenarios__mutmut_557, 
    'x_generate_complex_scenarios__mutmut_558': x_generate_complex_scenarios__mutmut_558, 
    'x_generate_complex_scenarios__mutmut_559': x_generate_complex_scenarios__mutmut_559, 
    'x_generate_complex_scenarios__mutmut_560': x_generate_complex_scenarios__mutmut_560, 
    'x_generate_complex_scenarios__mutmut_561': x_generate_complex_scenarios__mutmut_561, 
    'x_generate_complex_scenarios__mutmut_562': x_generate_complex_scenarios__mutmut_562, 
    'x_generate_complex_scenarios__mutmut_563': x_generate_complex_scenarios__mutmut_563, 
    'x_generate_complex_scenarios__mutmut_564': x_generate_complex_scenarios__mutmut_564, 
    'x_generate_complex_scenarios__mutmut_565': x_generate_complex_scenarios__mutmut_565, 
    'x_generate_complex_scenarios__mutmut_566': x_generate_complex_scenarios__mutmut_566, 
    'x_generate_complex_scenarios__mutmut_567': x_generate_complex_scenarios__mutmut_567, 
    'x_generate_complex_scenarios__mutmut_568': x_generate_complex_scenarios__mutmut_568, 
    'x_generate_complex_scenarios__mutmut_569': x_generate_complex_scenarios__mutmut_569, 
    'x_generate_complex_scenarios__mutmut_570': x_generate_complex_scenarios__mutmut_570, 
    'x_generate_complex_scenarios__mutmut_571': x_generate_complex_scenarios__mutmut_571, 
    'x_generate_complex_scenarios__mutmut_572': x_generate_complex_scenarios__mutmut_572, 
    'x_generate_complex_scenarios__mutmut_573': x_generate_complex_scenarios__mutmut_573, 
    'x_generate_complex_scenarios__mutmut_574': x_generate_complex_scenarios__mutmut_574, 
    'x_generate_complex_scenarios__mutmut_575': x_generate_complex_scenarios__mutmut_575, 
    'x_generate_complex_scenarios__mutmut_576': x_generate_complex_scenarios__mutmut_576, 
    'x_generate_complex_scenarios__mutmut_577': x_generate_complex_scenarios__mutmut_577, 
    'x_generate_complex_scenarios__mutmut_578': x_generate_complex_scenarios__mutmut_578, 
    'x_generate_complex_scenarios__mutmut_579': x_generate_complex_scenarios__mutmut_579, 
    'x_generate_complex_scenarios__mutmut_580': x_generate_complex_scenarios__mutmut_580, 
    'x_generate_complex_scenarios__mutmut_581': x_generate_complex_scenarios__mutmut_581, 
    'x_generate_complex_scenarios__mutmut_582': x_generate_complex_scenarios__mutmut_582, 
    'x_generate_complex_scenarios__mutmut_583': x_generate_complex_scenarios__mutmut_583, 
    'x_generate_complex_scenarios__mutmut_584': x_generate_complex_scenarios__mutmut_584, 
    'x_generate_complex_scenarios__mutmut_585': x_generate_complex_scenarios__mutmut_585, 
    'x_generate_complex_scenarios__mutmut_586': x_generate_complex_scenarios__mutmut_586, 
    'x_generate_complex_scenarios__mutmut_587': x_generate_complex_scenarios__mutmut_587, 
    'x_generate_complex_scenarios__mutmut_588': x_generate_complex_scenarios__mutmut_588, 
    'x_generate_complex_scenarios__mutmut_589': x_generate_complex_scenarios__mutmut_589, 
    'x_generate_complex_scenarios__mutmut_590': x_generate_complex_scenarios__mutmut_590, 
    'x_generate_complex_scenarios__mutmut_591': x_generate_complex_scenarios__mutmut_591, 
    'x_generate_complex_scenarios__mutmut_592': x_generate_complex_scenarios__mutmut_592, 
    'x_generate_complex_scenarios__mutmut_593': x_generate_complex_scenarios__mutmut_593, 
    'x_generate_complex_scenarios__mutmut_594': x_generate_complex_scenarios__mutmut_594, 
    'x_generate_complex_scenarios__mutmut_595': x_generate_complex_scenarios__mutmut_595, 
    'x_generate_complex_scenarios__mutmut_596': x_generate_complex_scenarios__mutmut_596, 
    'x_generate_complex_scenarios__mutmut_597': x_generate_complex_scenarios__mutmut_597, 
    'x_generate_complex_scenarios__mutmut_598': x_generate_complex_scenarios__mutmut_598, 
    'x_generate_complex_scenarios__mutmut_599': x_generate_complex_scenarios__mutmut_599, 
    'x_generate_complex_scenarios__mutmut_600': x_generate_complex_scenarios__mutmut_600, 
    'x_generate_complex_scenarios__mutmut_601': x_generate_complex_scenarios__mutmut_601, 
    'x_generate_complex_scenarios__mutmut_602': x_generate_complex_scenarios__mutmut_602, 
    'x_generate_complex_scenarios__mutmut_603': x_generate_complex_scenarios__mutmut_603, 
    'x_generate_complex_scenarios__mutmut_604': x_generate_complex_scenarios__mutmut_604, 
    'x_generate_complex_scenarios__mutmut_605': x_generate_complex_scenarios__mutmut_605, 
    'x_generate_complex_scenarios__mutmut_606': x_generate_complex_scenarios__mutmut_606, 
    'x_generate_complex_scenarios__mutmut_607': x_generate_complex_scenarios__mutmut_607, 
    'x_generate_complex_scenarios__mutmut_608': x_generate_complex_scenarios__mutmut_608, 
    'x_generate_complex_scenarios__mutmut_609': x_generate_complex_scenarios__mutmut_609, 
    'x_generate_complex_scenarios__mutmut_610': x_generate_complex_scenarios__mutmut_610, 
    'x_generate_complex_scenarios__mutmut_611': x_generate_complex_scenarios__mutmut_611, 
    'x_generate_complex_scenarios__mutmut_612': x_generate_complex_scenarios__mutmut_612, 
    'x_generate_complex_scenarios__mutmut_613': x_generate_complex_scenarios__mutmut_613, 
    'x_generate_complex_scenarios__mutmut_614': x_generate_complex_scenarios__mutmut_614, 
    'x_generate_complex_scenarios__mutmut_615': x_generate_complex_scenarios__mutmut_615, 
    'x_generate_complex_scenarios__mutmut_616': x_generate_complex_scenarios__mutmut_616, 
    'x_generate_complex_scenarios__mutmut_617': x_generate_complex_scenarios__mutmut_617, 
    'x_generate_complex_scenarios__mutmut_618': x_generate_complex_scenarios__mutmut_618, 
    'x_generate_complex_scenarios__mutmut_619': x_generate_complex_scenarios__mutmut_619, 
    'x_generate_complex_scenarios__mutmut_620': x_generate_complex_scenarios__mutmut_620, 
    'x_generate_complex_scenarios__mutmut_621': x_generate_complex_scenarios__mutmut_621, 
    'x_generate_complex_scenarios__mutmut_622': x_generate_complex_scenarios__mutmut_622, 
    'x_generate_complex_scenarios__mutmut_623': x_generate_complex_scenarios__mutmut_623, 
    'x_generate_complex_scenarios__mutmut_624': x_generate_complex_scenarios__mutmut_624, 
    'x_generate_complex_scenarios__mutmut_625': x_generate_complex_scenarios__mutmut_625, 
    'x_generate_complex_scenarios__mutmut_626': x_generate_complex_scenarios__mutmut_626, 
    'x_generate_complex_scenarios__mutmut_627': x_generate_complex_scenarios__mutmut_627, 
    'x_generate_complex_scenarios__mutmut_628': x_generate_complex_scenarios__mutmut_628, 
    'x_generate_complex_scenarios__mutmut_629': x_generate_complex_scenarios__mutmut_629, 
    'x_generate_complex_scenarios__mutmut_630': x_generate_complex_scenarios__mutmut_630, 
    'x_generate_complex_scenarios__mutmut_631': x_generate_complex_scenarios__mutmut_631, 
    'x_generate_complex_scenarios__mutmut_632': x_generate_complex_scenarios__mutmut_632, 
    'x_generate_complex_scenarios__mutmut_633': x_generate_complex_scenarios__mutmut_633, 
    'x_generate_complex_scenarios__mutmut_634': x_generate_complex_scenarios__mutmut_634, 
    'x_generate_complex_scenarios__mutmut_635': x_generate_complex_scenarios__mutmut_635, 
    'x_generate_complex_scenarios__mutmut_636': x_generate_complex_scenarios__mutmut_636, 
    'x_generate_complex_scenarios__mutmut_637': x_generate_complex_scenarios__mutmut_637, 
    'x_generate_complex_scenarios__mutmut_638': x_generate_complex_scenarios__mutmut_638, 
    'x_generate_complex_scenarios__mutmut_639': x_generate_complex_scenarios__mutmut_639, 
    'x_generate_complex_scenarios__mutmut_640': x_generate_complex_scenarios__mutmut_640, 
    'x_generate_complex_scenarios__mutmut_641': x_generate_complex_scenarios__mutmut_641, 
    'x_generate_complex_scenarios__mutmut_642': x_generate_complex_scenarios__mutmut_642, 
    'x_generate_complex_scenarios__mutmut_643': x_generate_complex_scenarios__mutmut_643, 
    'x_generate_complex_scenarios__mutmut_644': x_generate_complex_scenarios__mutmut_644, 
    'x_generate_complex_scenarios__mutmut_645': x_generate_complex_scenarios__mutmut_645, 
    'x_generate_complex_scenarios__mutmut_646': x_generate_complex_scenarios__mutmut_646, 
    'x_generate_complex_scenarios__mutmut_647': x_generate_complex_scenarios__mutmut_647, 
    'x_generate_complex_scenarios__mutmut_648': x_generate_complex_scenarios__mutmut_648, 
    'x_generate_complex_scenarios__mutmut_649': x_generate_complex_scenarios__mutmut_649
}

def generate_complex_scenarios(*args, **kwargs):
    result = _mutmut_trampoline(x_generate_complex_scenarios__mutmut_orig, x_generate_complex_scenarios__mutmut_mutants, args, kwargs)
    return result 

generate_complex_scenarios.__signature__ = _mutmut_signature(x_generate_complex_scenarios__mutmut_orig)
x_generate_complex_scenarios__mutmut_orig.__name__ = 'x_generate_complex_scenarios'


def x_get_scenario_statistics__mutmut_orig(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_1(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_2(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = None
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_3(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = None

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_4(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "XXcountXX": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_5(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "COUNT": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_6(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "XXavg_ambiguityXX": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_7(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "AVG_AMBIGUITY": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_8(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities) * len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_9(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(None)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_10(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "XXavg_conflicting_signalsXX": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_11(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "AVG_CONFLICTING_SIGNALS": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_12(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities) * len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_13(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(None)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_14(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "XXavg_rule_coverageXX": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_15(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "AVG_RULE_COVERAGE": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_16(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities) * len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_17(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(None)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_18(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "XXdecision_distributionXX": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_19(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "DECISION_DISTRIBUTION": {
            decision.value: sum(1 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_20(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(None)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_21(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(2 for d in decisions if d == decision)
            for decision in ComplianceDecision
        },
    }


def x_get_scenario_statistics__mutmut_22(
    scenarios: List[Tuple[AuditResult, ComplianceDecision, ScenarioComplexity]],
) -> dict:
    """
    Compute statistics about generated scenarios.

    Args:
        scenarios: List of (audit, decision, complexity) tuples

    Returns:
        Dict with statistics
    """
    if not scenarios:
        return {}

    complexities = [c for _, _, c in scenarios]
    decisions = [d for _, d, _ in scenarios]

    return {
        "count": len(scenarios),
        "avg_ambiguity": sum(c.ambiguity_score for c in complexities)
        / len(complexities),
        "avg_conflicting_signals": sum(c.conflicting_signals for c in complexities)
        / len(complexities),
        "avg_rule_coverage": sum(c.rule_coverage for c in complexities)
        / len(complexities),
        "decision_distribution": {
            decision.value: sum(1 for d in decisions if d != decision)
            for decision in ComplianceDecision
        },
    }

x_get_scenario_statistics__mutmut_mutants : ClassVar[MutantDict] = {
'x_get_scenario_statistics__mutmut_1': x_get_scenario_statistics__mutmut_1, 
    'x_get_scenario_statistics__mutmut_2': x_get_scenario_statistics__mutmut_2, 
    'x_get_scenario_statistics__mutmut_3': x_get_scenario_statistics__mutmut_3, 
    'x_get_scenario_statistics__mutmut_4': x_get_scenario_statistics__mutmut_4, 
    'x_get_scenario_statistics__mutmut_5': x_get_scenario_statistics__mutmut_5, 
    'x_get_scenario_statistics__mutmut_6': x_get_scenario_statistics__mutmut_6, 
    'x_get_scenario_statistics__mutmut_7': x_get_scenario_statistics__mutmut_7, 
    'x_get_scenario_statistics__mutmut_8': x_get_scenario_statistics__mutmut_8, 
    'x_get_scenario_statistics__mutmut_9': x_get_scenario_statistics__mutmut_9, 
    'x_get_scenario_statistics__mutmut_10': x_get_scenario_statistics__mutmut_10, 
    'x_get_scenario_statistics__mutmut_11': x_get_scenario_statistics__mutmut_11, 
    'x_get_scenario_statistics__mutmut_12': x_get_scenario_statistics__mutmut_12, 
    'x_get_scenario_statistics__mutmut_13': x_get_scenario_statistics__mutmut_13, 
    'x_get_scenario_statistics__mutmut_14': x_get_scenario_statistics__mutmut_14, 
    'x_get_scenario_statistics__mutmut_15': x_get_scenario_statistics__mutmut_15, 
    'x_get_scenario_statistics__mutmut_16': x_get_scenario_statistics__mutmut_16, 
    'x_get_scenario_statistics__mutmut_17': x_get_scenario_statistics__mutmut_17, 
    'x_get_scenario_statistics__mutmut_18': x_get_scenario_statistics__mutmut_18, 
    'x_get_scenario_statistics__mutmut_19': x_get_scenario_statistics__mutmut_19, 
    'x_get_scenario_statistics__mutmut_20': x_get_scenario_statistics__mutmut_20, 
    'x_get_scenario_statistics__mutmut_21': x_get_scenario_statistics__mutmut_21, 
    'x_get_scenario_statistics__mutmut_22': x_get_scenario_statistics__mutmut_22
}

def get_scenario_statistics(*args, **kwargs):
    result = _mutmut_trampoline(x_get_scenario_statistics__mutmut_orig, x_get_scenario_statistics__mutmut_mutants, args, kwargs)
    return result 

get_scenario_statistics.__signature__ = _mutmut_signature(x_get_scenario_statistics__mutmut_orig)
x_get_scenario_statistics__mutmut_orig.__name__ = 'x_get_scenario_statistics'
