apiVersion: v1
kind: ResourceQuota
metadata:
  name: codex-ml-quota
  labels:
    app: codex-ml
spec:
  hard:
    requests.cpu: "20"
    requests.memory: "40Gi"
    limits.cpu: "40"
    limits.memory: "80Gi"
    persistentvolumeclaims: "10"
    pods: "50"
