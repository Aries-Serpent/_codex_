# Orchestrator Prompt — Golden Harness: GREEN

The golden harness reports **GREEN**. Proceed with standard Codex execution while maintaining offline posture.

- Honor existing RA and regression policies.
- Continue recording honesty statements for all user-visible claims.
- Keep tool trace logging enabled for every local tool invocation (pytest, nox, semgrep, custom scripts).
- Summarize final artifacts, including `golden_harness_status.json`, `artifacts/honesty_metadata.json`, and `artifacts/tool_trace.ndjson`.
