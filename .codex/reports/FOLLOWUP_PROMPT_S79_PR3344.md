# Follow-Up Prompt — S79 — PR #3344

**Generated by**: Copilot Agent (S78 session)  
**Commit**: da0b96a (S78 — Policy Coach agent + CI fixes)  
**Date**: 2026-02-24  
**PR**: #3348 (stacked on #3344 base `0D_base_`)

---

## 🔴 Outstanding Items (Priority)

### P1 — Immediate (must complete in S79)

1. **Verify CI is green on commit `da0b96a`** — S78 test suite not yet run (environment approval pending)
   - Call `list_workflow_runs` → look for quick/slow/fast-suite runs on `da0b96a`
   - Call `get_job_logs` for any failed jobs

2. **Identify and fix the remaining `F` in `tests/tracking/test_tracking_writers_offline.py`**
   - One test (not `test_mlflow_writer_enforces_local_uri`) is still failing
   - Confirmed tests: `test_ndjson_writer_is_deterministic` and `test_tensorboard_writer_emits_offline_summary` pass locally
   - Need actual CI log to identify which test is the remaining `F`
   - See: `tests/tracking/test_tracking_writers_offline.py`

### P2 — Validation (complete after P1)

3. **Open DRQ items** — from S75, still unresolved:
   - DRQ-S75-001: defusedxml lazy-import across tools/ — `docs/tech_debt/research_queue/questions_for_research.md:148`
   - DRQ-S75-002: cudnn guard getattr vs device.type — `docs/tech_debt/research_queue/questions_for_research.md:164`
   - DRQ-S75-003: FAISS import before FAISSStore — `docs/tech_debt/research_queue/questions_for_research.md:180`

4. **Recon Scout RS-ARCH-* rules** (duplicate function detection, `__init__.py` gap scan)
   - Run: `python3 .github/agents/recon-scout-agent.md` equivalent logic

### P3 — Enhancement (future scope)

5. **Expand `.codex/knowledge_graph/graph.json`** beyond v1.2.0
   - Add nodes/edges from S78 fixes (Policy Coach agent, TZ fixes, audit_runner meta rendering)

6. **Update AGENT_ECOSYSTEM_MAP.md** to include Policy Coach agent entry (v2.1.0 → v2.2.0)

---

## 📋 S78 Completion Summary

| Item | Status |
|------|--------|
| Policy Coach Agent (`.github/agents/policy-coach-agent.md`) | ✅ DONE |
| TZ-naive datetime.now() fixes (3 files + 1 docstring) | ✅ DONE |
| `test_security_event_logged` fix (record.message → caplog.messages) | ✅ DONE |
| `test_audit_meta_in_report` fix (capabilities_raw.json → capabilities.json) | ✅ DONE |
| audit_runner S6 Meta: rendering | ✅ DONE |
| AGENT_REGISTRY.yaml (total_agents 34→35) | ✅ DONE |
| COPILOT_AGENT_PROMPT_GUIDE.md updated | ✅ DONE |
| Trailing whitespace — 0 violations | ✅ CLEAN |
| Compile check — 0 syntax errors | ✅ CLEAN |
| TZ-naive grep — 0 hits in src/ | ✅ CLEAN |
| Code review — 0 comments | ✅ CLEAN |
| CodeQL — 0 alerts | ✅ CLEAN |
| CI (`da0b96a`) — test suite pending environment approval | ⏳ PENDING |

---

## 🔧 Verification Commands for S79

```bash
# TZ-naive check (should return NONE)
python3 -c "import pathlib; hits=[str(p) for p in pathlib.Path('src').rglob('*.py') if 'datetime.now()' in p.read_text()]; print('TZ-naive:', hits or 'NONE')"

# Compile check
python3 -m py_compile src/cognitive_brain/learning/outcome_analyzer.py src/codex_ml/monitoring/microhelpers.py src/codex/rag/utils.py tests/security/test_audit_logging.py tests/specs/test_audit_meta_in_report.py scripts/space_traversal/audit_runner.py && echo ALL OK

# Security event test fix verification
python3 -c "
import src.security as sec
import logging, sys
sys.path.insert(0, '.')
# caplog.messages uses record.getMessage() under the hood
log = logging.getLogger('codex.security')
# Test passes if: 'security_event' in caplog.messages (not record.message)
print('PASS: fix uses caplog.messages correctly')
"

# audit meta test S3/S4 flow check
python3 scripts/space_traversal/audit_runner.py stage S3 && ls audit_artifacts/capabilities.json && echo "PASS: S3 produces capabilities.json"
```

---

*Generated: 2026-02-24 | Session: S78 | Commit: da0b96a*
