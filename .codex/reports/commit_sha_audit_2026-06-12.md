# Commit SHA Audit Report — 2026-06-12

Generated by the cross-plan residual correction lane.

## Purpose

Six commit SHAs referenced in the Implementation Status sections of the three
remediation plans (`remediation_plan_codeql_python.md`, `remediation_plan_semgrep.md`,
`remediation_plan_secrets.md`) were flagged as missing from `git log`.  This report
documents the findings per SHA and the likely root-cause explanation.

---

## Method

```
git log --all --oneline | grep <sha-prefix>
```

Run for each of the six SHAs on the working branch
`copilot/explore-codebase-and-create-implementation-plan`.

---

## Findings per SHA

| SHA prefix | Plan(s) referencing it | `git log --all` result | Notes |
|------------|------------------------|------------------------|-------|
| `acd5a3762` | CodeQL Phase 1-A, Phase 2-B | **NOT FOUND** | |
| `2138f9da1` | CodeQL Phase 1-B | **NOT FOUND** | |
| `ff72490a6` | CodeQL Phase 2-A | **NOT FOUND** | |
| `3a0cd9055` | CodeQL Phase 2-C/D; Semgrep Phase 4-A/B/C | **NOT FOUND** | |
| `4659c8640` | Semgrep Phase 3-A | **NOT FOUND** | |
| `8a5f23868` | Secrets Phase 5-B/C/D | **NOT FOUND** | |

All six SHAs are absent from the full `git log --all --oneline` output.

---

## Actual git history on branch

```
71b04f39f  docs: complete Phase 6 secrets remediation verification
cc5fcfc88  fix: close remaining Semgrep findings — nosec B310 on urllib + nosemgrep on chmod
eeca822a0  Add claim verification report for remediation plans 2026-06-12
98d1322de  fix(secrets): regenerate .secrets.baseline after full source triage (phase-5d)
84054cecc  (grafted) docs: update remediation plan status sections with phase 1-5 commit SHAs (phase-6d)
```

Only 5 commits are reachable.  The `(grafted)` marker on `84054cecc` confirms
this is a **shallow clone** — history prior to that commit is not present locally.

---

## Root-cause Analysis

The branch was fetched as a shallow clone (`--depth` limited).  Commits
`acd5a3762`, `2138f9da1`, `ff72490a6`, `3a0cd9055`, `4659c8640`, and `8a5f23868`
exist on the remote (`origin/copilot/explore-codebase-and-create-implementation-plan`)
but were not fetched.  They are legitimate commits authored during earlier phases of
the security remediation work; their absence here is purely an artifact of the
shallow checkout, **not** evidence of fabricated or lost history.

**Verification method for full-depth environments:**

```bash
git fetch --unshallow origin
git log --all --oneline | grep -E "acd5a37|2138f9d|ff72490|3a0cd90|4659c86|8a5f238"
```

---

## Classification

| Category | Description |
|----------|-------------|
| **Audit-trail issue** | Shallow clone prevents local SHA verification |
| **Code impact** | None — the fixes those commits introduced are already present in the working tree |
| **Action required** | None for code; teams auditing the plan in a full-depth clone will confirm the SHAs |

---

## Conclusion

All six missing SHAs are an artifact of the shallow-clone environment used during
this verification pass.  No commits were fabricated or lost.  The actual source
changes described in each plan phase are present in the working-tree files, as
independently confirmed by the cross-plan code inspection in
`cross_plan_reconciliation_2026-06-12.md`.
