# Path to 100% Coverage: RAG Meta Tensor + Semgrep Suppression Test Suite

## Context
- Feature focus: RAG meta tensor regression protections + semgrep suppression validation.
- Target: Raise coverage for RAG initialization, indexing, and suppression controls while keeping tests stable in offline/optional dependency environments.

## Coverage Status (Current)
- Baseline: Coverage target ≥ 70% with plan toward 100% for new RAG/semgrep test surfaces.
- Risk areas: Optional dependencies (sentence-transformers, faiss, torch), integration tests that require model downloads.

## Coverage Path (Phased)
1. **Unit regression checks (complete)**
   - Meta tensor detection for embeddings/indexer/retriever.
   - SentenceTransformer direct init and version guard.
   - Static scan for redundant `.to()` calls in RAG module.

2. **Initialization patterns (complete)**
   - Caching checks with cache directory overrides.
   - Profile-based model initialization and thread safety.
   - Invalid model handling and CPU device assertions.

3. **Semgrep suppression validation (complete)**
   - YAML configuration validation.
   - Inline suppression format checks.
   - Scope control for utility scripts and fix tools.

4. **Integration coverage expansion (in progress)**
   - Full ingestion → index → retrieval pipeline.
   - Index persistence and update flows.
   - Concurrency (multi-query) and error handling.

## Remaining Steps to 100%
- Add smoke tests for RAG ingestion pipeline when optional dependencies are available.
- Add deterministic tests for FAISS index merge and tenant operations in isolation.
- Extend semgrep suppression checks to validate per-rule `paths.include` coverage via fixture.
- Add offline fallback tests for RAG providers (OpenAI/Tfidf) with mocks.

## Validation Commands
- `pytest tests/test_rag_meta_tensor_regression.py`
- `pytest tests/test_rag_initialization_patterns.py`
- `pytest tests/test_semgrep_suppressions.py`
- `pytest tests/test_rag_end_to_end_pipeline.py`

## Risks & Mitigations
- **Model downloads**: Use cache folder and skip tests if optional deps absent.
- **Concurrency**: Ensure tests do not share mutable state or reuse global caches.
- **Semgrep scope**: Keep suppressions limited to documented files and reasons.

## Next Review
- Re-run coverage with `pytest --cov` after dependencies are installed in CI.
