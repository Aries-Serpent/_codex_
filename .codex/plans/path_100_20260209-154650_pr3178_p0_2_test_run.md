# Path to 100% Coverage - PR3178 P0.2 Test Run

Date: 2026-02-09
Focus: Restore full test execution to enable coverage recovery and failure categorization.

## Current State
- Full suite collection halted due to 149 import/config errors.
- Missing runtime dependencies prevent test execution (numpy, PyYAML, hydra-core, mlflow, torch).
- Coverage targets cannot be measured until test collection completes.

## Goal
- Achieve clean collection and execute >70% of the suite within this environment.
- Capture baseline coverage and identify modules below 70%.
- Prepare targeted fixes for P0.3 and P1 stabilization.

## Step Plan
1. **Dependency remediation**
   - Install missing dependencies: numpy, pyyaml, hydra-core==1.3.2, mlflow, torch (via requirements.txt for CPU-only wheels).
   - Re-run P0.1 verification to ensure fixtures and resource guards still load.

2. **Collection sanity check**
   - Run `pytest tests/ -m "not slow" --maxfail=1 --tb=short` to confirm clean collection.
   - Capture any residual collection errors with logs in `.codex/`.

3. **Full suite execution**
   - Re-run the full test suite with timeout and tee logging.
   - Extract pass/fail/error counts and update P0.2 results.

4. **Coverage recovery**
   - Execute coverage run once collection succeeds: `pytest --cov=src --cov-report=term-missing --cov-fail-under=70`.
   - Record coverage gaps and prioritize remediation in P1.

5. **Failure categorization (P0.3)**
   - Categorize failures by root cause (import, config, API mismatch, assertion) and assign fixes.

## Risks & Mitigations
- **Large dependency footprint**: Install in phases (core deps first) to avoid timeouts.
- **Environment mismatch**: Track PYENV_VERSION and dependency versions in `.codex/` logs for repeatability.

## Notes
This plan will be updated after dependency remediation to reflect actual coverage metrics and prioritized fixes.
