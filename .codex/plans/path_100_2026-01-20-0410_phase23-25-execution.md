# Path to 100% Coverage: Phase 23-25 Execution (Best-Effort Continuation)

**Date**: 2026-01-20 04:10 UTC  
**Feature**: Phase 23-25 coverage execution continuation  
**Owner**: Codex agent (best-effort)  
**Status**: 🚧 IN PROGRESS (requires multi-week execution)

## Context
The Phase 23-25 plansets define a multi-week execution path to move coverage from ~17% to 70%. This session produces a concrete continuation plan that:
- Aligns with PDA (Plan → Do → Analyze) loops.
- Defines weekly test targets and validation commands.
- Captures rollback and remediation steps for failures.
- Provides a path to 100% coverage beyond Phase 25.

## PDA Loop — Week-Level Execution Plan

### Week 1 (Phase 23) — Unit Test Foundation
**Plan**: Focus on CLI, data loaders, training config, registry utils. Target: +120-150 unit tests.

**Do**: Add unit tests for CLI argument parsing, data split utilities, registry lookups.

**Analyze**: Run `pytest -q tests/unit` and coverage report. Record failures.

### Week 2 (Phase 23) — Integration Tests
**Plan**: Focus on CLI → pipeline, data → model pipeline. Target: +100-120 integration tests.

**Do**: Add integration tests for CLI → pipeline init, data ingestion → training loop.

**Analyze**: Run `pytest -q tests/integration`. Collect coverage and CI metrics.

### Week 3 (Phase 23) — Threshold to 30%
**Plan**: Fill coverage gaps. Target: +30-50 tests. Update `pyproject.toml` `fail_under=30`.

**Do**: Add tests for untested utilities and error branches.

**Analyze**: `pytest --cov` with fail-under enforcement. 3 consecutive CI passes.

## Phase 24 (Coverage 50%) — Integration + Workflow/E2E
- Week 1: 100-120 integration tests (cross-module workflows).
- Week 2: 80-100 workflow/E2E tests.
- Update `pyproject.toml` `fail_under=50` after validation.

## Phase 25 (Coverage 70%) — Production Ready
- Week 1: 80-100 critical path tests.
- Week 2: Edge cases + robustness tests.
- Update `pyproject.toml` `fail_under=70`.

## Beyond Phase 25 — Path to 100%
- Maintain weekly PDA loop additions.
- Expand property-based testing for core utilities.
- Add fuzz tests for CLI/API inputs.
- Integrate chaos tests for resilience.

## Next Actions (Immediate)
1. Review plansets for Phase 23-25 to align module targets.
2. Begin Phase 23 Week 1 test additions.
3. Log updates in action_log, change_log, results.
