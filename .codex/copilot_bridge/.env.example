# Bridge bind/port
BRIDGE_BIND=127.0.0.1
BRIDGE_PORT=7777

# Defaults
DEFAULT_CWD=/srv/copilot-bridge/repo
DEFAULT_TIMEOUT_MS=600000

# Tooling policy (deny-by-default beyond allowed set)
ALLOW_ALL_TOOLS=false
DEFAULT_ALLOW_TOOLS=shell,git,gh,write
DEFAULT_DENY_TOOLS=shell(rm),shell(sudo),shell(dd),shell(curl -X POST),shell(wget),shell(docker push)

# Paths (default to .codex/copilot_bridge/var within the project)
LOG_DIR=./var/logs
MANIFEST_DIR=./var/manifests

############################################################
# Node Strategy (Unified vs Scoped) — choose ONE:
#
# 1) Unified (preferred): install Node 22 system-wide or via nvm/asdf
#    so `node -v` prints v22.x.y and no PATH override is needed.
#
# 2) Scoped fallback: leave the host default alone, but force the
#    *service* to use Node 22 by placing its bin dir first on PATH.
#    Uncomment and set as needed (example shown):
#
# PATH=/opt/node-v22/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin
#
# The systemd unit uses /etc/copilot-bridge/env (this file template).
############################################################
