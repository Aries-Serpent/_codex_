extends: default

rules:
  line-length:
    max: 140
    level: warning
  truthy:
    level: warning
  document-start:
    present: false
  # Allow aligned env variable blocks in GitHub Actions workflows (e.g., padded colon spacing)
  colons:
    max-spaces-after: -1
    max-spaces-before: 0
