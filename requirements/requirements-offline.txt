# Phase 8: Offline-Only Requirements
# Purpose: Minimal dependencies for offline-first operation
# These are the core dependencies that work in air-gap environments
#
# Installation: pip install --no-index --find-links ./wheelhouse -r requirements-offline.txt
#
# All network-dependent packages excluded:
# - torch (optional, will fallback to CPU inference)
# - transformers (optional, uses local models)
# - requests (optional, network-first only)
# - huggingface-hub (optional, requires network)

# Essential configuration & validation (100% offline-viable)
omegaconf>=2.3.1
hydra-core==1.3.5
pydantic>=2.13.4
pydantic-settings>=2.15.0
pyyaml>=6.0
marshmallow>=4.3.1,<5

# CLI support
typer>=0.27.1

# Code analysis (offline, no network calls)
libcst>=1.9.0
parso>=0.8.7
radon>=6.0.1
jinja2>=3.1.6

# Security (cryptography, no network calls)
cryptography>=50.0.1,<51.0.0
PyJWT>=2.13.0,<3.0.0
PyNaCl>=1.6.2,<2.0.0

# Utilities & standards
jsonschema>=4.26.0
tomli>=2.0; python_version < "3.11"

# Logging & monitoring (local only)
python-json-logger>=4.2.0
rich>=13.0.0

# Data structures (no network)
attrs>=23.0.0

# Memory management (offline)
filelock>=3.32.4
