# Optional dependencies for Codex ML framework
#
# These dependencies are not required for core functionality but enable
# additional features like advanced metrics, LoRA adapters, and distributed training.
#
# Install with: pip install -r requirements-optional.txt
# Or selectively: pip install nltk rouge-score sacrebleu

# Generative metrics (BLEU, ROUGE)
nltk>=3.10.3  # Security: Fixes CVE-2025-14009 (ZIP extraction RCE), path traversal in corpus readers
rouge-score>=0.1.2
sacrebleu>=2.6.0

# LoRA/PEFT adapters
peft>=0.20.0,<1
accelerate>=1.14.0,<2

# Distributed training
torch-distributed>=2.0.0  # Usually bundled with pytorch

# Advanced model features
transformers>=5.16.1,<6  # Security: Updated from 4.30.0 to fix deserialization vulnerabilities
sentencepiece>=0.2.2

# Vector stores (stubbed - see docs/data/VectorStores.md)
# pgvector  # Uncomment if using PostgreSQL vector store
# weaviate-client  # Uncomment if using Weaviate

# Monitoring and tracking
tensorboard>=2.13.0
wandb>=0.29.0
opentelemetry-sdk>=1.44.0

# Security: IP-005 dependency updates
twisted>=26.4.0  # Security: Phase 5 Track 2 - Updated to fix PYSEC-2026-160 (DNS name decompression DoS in twisted <26.4.0)
configobj>=5.0.9  # Security: Fixes CVE-2023-26112 (ReDoS)
