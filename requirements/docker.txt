# Minimal runtime pins for container (conservative)
# If the project already pins these via pyproject / requirements/base.txt, you may remove this file.
fastapi>=0.95
uvicorn[standard]>=0.22
pydantic>=1.10
requests>=2.31
