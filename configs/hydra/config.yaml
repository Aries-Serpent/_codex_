defaults:
- training/base
- model/base
- data/base
- _self_
hydra:
  searchpath:
  - pkg://configs
  - file://configs
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
env: development
