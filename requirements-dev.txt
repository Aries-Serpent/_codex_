# Development & CI dependencies (normalized)
# Core test tooling
pytest>=7.4.4
pytest-cov>=4.1.0
mypy>=1.10.0
ruff>=0.6.2
black>=24.10.0

# Security scanners installed via pip (binary tools installed in CI)
pip-audit>=2.7.0
bandit>=1.7.5
# gitleaks is a standalone binary; do NOT list here (installed in CI job)

# Types
# types-tomli not needed for Python 3.12+ (tomllib is in stdlib)
types-jsonschema

# Runtime/test deps commonly required by tests
pydantic>=2.5.0
hydra-core>=1.3.2
defusedxml>=0.7.1
jsonschema>=4.22.0
requests>=2.31.0
sentencepiece>=0.1.99

# ML stack (CPU) — pin coherent versions
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1
transformers>=4.38.0
accelerate>=0.29.0
