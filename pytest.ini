[pytest]
# Removed global coverage flags to prevent duplication with nox 'coverage' session.
# Fast local runs can use 'nox -s tests'. CI coverage uses 'nox -s coverage'.
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    smoke
    determinism
    gpu
    eval
    training
    regression