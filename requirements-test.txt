# Pinned test dependencies for reproducible CI and Docker builds
# Maintainer note: These versions are pinned for deterministic builds.
# Update with care and test thoroughly before merging changes.

# Include base project dependencies first
# Installation order: Install requirements.txt first, then requirements-test.txt
#   pip install -r requirements.txt
#   pip install -r requirements-test.txt

# Test tooling (pinned versions for reproducibility)
pytest==8.0.0
pytest-cov==4.1.0
pytest-randomly==3.15.0
coverage[toml]==7.3.0
hypothesis>=6.100
