# Pinned test dependencies for reproducible CI and Docker builds
# Maintainer note: These versions are pinned for deterministic builds.
# Update with care and test thoroughly before merging changes.

# Include base project dependencies first
# Installation order: Install requirements.txt first, then requirements-test.txt
#   pip install --no-cache-dir -r requirements.txt
#   pip install --no-cache-dir -r requirements-test.txt
# This dependency chain ensures test deps have access to base project dependencies.
# If you modify requirements.txt independently, re-test with requirements-test.txt.

# Test tooling (pinned versions for reproducibility)
pytest==9.0.3
pytest-cov==5.0.0
pytest-xdist==3.8.0
pytest-randomly==4.0.1
pytest-rerunfailures==14.0
pytest-timeout==2.4.0
coverage[toml]>=7.10.6,<8
hypothesis>=6.152.4
responses==0.26.1
slowapi==0.1.9
hydra-core==1.3.2
mlflow>=2.22.4,<4   # Security: Updated to 2.22.4+ for broader compatibility; CVE fixes in 3.11.1 available

# RAG module dependencies for integration testing
# These are required for RAG tests that were previously skipped
sentence-transformers>=5.5.1  # Embedding models for RAG indexing/retrieval
faiss-cpu>=1.7.4              # Vector similarity search for RAG retrieval
openai>=2.38.0                 # OpenAI API client for RAG embeddings provider
