<<<<<<< HEAD
=======
---
>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
name: Adaptive Agent Delegation Framework

on:
  workflow_call:
    inputs:
      context_manifest:
        default: .codex/session_context_manifest.json
        type: string
      delegation_mode:
        default: parallel
        type: string
      max_agents:
        default: '5'
        type: string
  workflow_dispatch:
    inputs:
      context_manifest:
        default: .codex/session_context_manifest.json
        description: Path to session_context_manifest.json
        required: false
      delegation_mode:
        default: parallel
        description: Delegation mode
        options:
          - sequential
          - parallel
          - cascading
        required: true
        type: choice
      dry_run:
        default: false
        description: Dry run (no actual delegation)
        type: boolean
      max_agents:
        default: '5'
        description: Maximum agents to run in parallel
        required: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  load_context:
    name: Load Aggregated Context
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.CODEX_MASTER_KEY || secrets.CODEX_BACKUP_KEY }}
    outputs:
      manifest_content: ${{ steps.load.outputs.manifest }}
      agents_to_delegate: ${{ steps.parse.outputs.agents }}
      delegation_count: ${{ steps.parse.outputs.count }}
      has_critical: ${{ steps.parse.outputs.has_critical }}
    steps:
      - name: Configure secret masking
        run: |
          echo '::add-mask::' "${{ secrets.CODEX_MASTER_KEY }}"
          echo '::add-mask::' "${{ secrets.CODEX_BACKUP_KEY }}"
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: load
        name: Load context manifest
        env:
          MANIFEST_PATH: ${{ inputs.context_manifest || '.codex/session_context_manifest.json' }}
        run: |
          if [ -f "$MANIFEST_PATH" ]; then
            echo "manifest=$(cat "$MANIFEST_PATH" | jq -c .)" >> $GITHUB_OUTPUT
            echo "✅ Loaded context from $MANIFEST_PATH"
          else
            echo "⚠️ Manifest not found at $MANIFEST_PATH, using empty context"
            echo "manifest={}" >> $GITHUB_OUTPUT
          fi
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: parse
        name: Parse delegation recommendations
        env:
          MANIFEST: ${{ steps.load.outputs.manifest }}
        run: |
          AGENTS=$(echo "$MANIFEST" | jq -r '.delegation_recommendations[0].recommended_agents[]?' 2>/dev/null | tr '\n' ',' | sed 's/,$//')
<<<<<<< HEAD
          
          if [ -z "$AGENTS" ]; then
            AGENTS="unified-coverage-agent,ci-auto-healer-agent,workflow-health-monitor"
          fi
          
          AGENT_COUNT=$(echo "$AGENTS" | tr ',' '\n' | wc -l)
          MAX_AGENTS="${{ inputs.max_agents || 5 }}"
          
          if [ "$AGENT_COUNT" -gt "$MAX_AGENTS" ]; then
            AGENTS=$(echo "$AGENTS" | tr ',' '\n' | head -n "$MAX_AGENTS" | tr '\n' ',')
          fi
          
          echo "agents=$AGENTS" >> $GITHUB_OUTPUT
          echo "count=$AGENT_COUNT" >> $GITHUB_OUTPUT
          
          HAS_CRITICAL=$(echo "$AGENTS" | grep -c "security\|auth\|critical" || echo "0")
          echo "has_critical=$HAS_CRITICAL" >> $GITHUB_OUTPUT
          
          echo "📋 Delegating to agents: $AGENTS"
      
=======

          if [ -z "$AGENTS" ]; then
            AGENTS="unified-coverage-agent,ci-auto-healer-agent,workflow-health-monitor"
          fi

          AGENT_COUNT=$(echo "$AGENTS" | tr ',' '\n' | wc -l)
          MAX_AGENTS="${{ inputs.max_agents || 5 }}"

          if [ "$AGENT_COUNT" -gt "$MAX_AGENTS" ]; then
            AGENTS=$(echo "$AGENTS" | tr ',' '\n' | head -n "$MAX_AGENTS" | tr '\n' ',')
          fi

          echo "agents=$AGENTS" >> $GITHUB_OUTPUT
          echo "count=$AGENT_COUNT" >> $GITHUB_OUTPUT

          HAS_CRITICAL=$(echo "$AGENTS" | grep -c "security\|auth\|critical" || echo "0")
          echo "has_critical=$HAS_CRITICAL" >> $GITHUB_OUTPUT

          echo "📋 Delegating to agents: $AGENTS"

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Summary
        run: |
          echo "## Context Load Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Manifest Path:** ${{ inputs.context_manifest || '.codex/session_context_manifest.json' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** Loaded" >> $GITHUB_STEP_SUMMARY

  delegate_agents:
    if: ${{ needs.load_context.outputs.delegation_count > 0 }}
    name: Delegate to ${{ matrix.agent }}
    needs: load_context
    runs-on: ubuntu-latest
    strategy:
      matrix:
        agent: ${{ fromJson(format('[{0}]', needs.load_context.outputs.agents_to_delegate)) }}
      max-parallel: ${{ inputs.max_agents || 5 }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: init
        name: Initialize delegation context
        env:
          AGENT: ${{ matrix.agent }}
          DRY_RUN: ${{ inputs.dry_run || false }}
          DELEGATION_MODE: ${{ inputs.delegation_mode || 'parallel' }}
        run: |
          echo "🚀 Delegating to agent: $AGENT"
          echo "   Mode: $DELEGATION_MODE"
          echo "   Dry run: $DRY_RUN"
          echo "agent_name=$AGENT" >> $GITHUB_OUTPUT
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: task_manifest
        name: Generate agent task manifest
        env:
          AGENT_ID: ${{ matrix.agent }}
          DELEGATION_MODE: ${{ inputs.delegation_mode || 'parallel' }}
          DRY_RUN: ${{ inputs.dry_run || false }}
          MANIFEST_CONTENT: ${{ needs.load_context.outputs.manifest_content }}
        run: |
          cat > /tmp/agent_task.json << 'EOF'
          {
            "agent_id": "AGENT_ID_PLACEHOLDER",
            "task_type": "adaptive_delegation",
            "phase": "phase_2_implementation",
            "context": {
              "current_phase": "MANIFEST_PLACEHOLDER",
              "delegation_mode": "MODE_PLACEHOLDER",
              "dry_run": "DRY_RUN_PLACEHOLDER"
            },
            "expected_outputs": [
              "agent_execution_summary",
              "results_artifact",
              "completion_status"
            ]
          }
          EOF
<<<<<<< HEAD
          
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
          # Safe substitution using sed
          sed -i "s|AGENT_ID_PLACEHOLDER|$AGENT_ID|g" /tmp/agent_task.json
          sed -i "s|MANIFEST_PLACEHOLDER|${MANIFEST_CONTENT//|/\\|}|g" /tmp/agent_task.json
          sed -i "s|MODE_PLACEHOLDER|$DELEGATION_MODE|g" /tmp/agent_task.json
          sed -i "s|DRY_RUN_PLACEHOLDER|$DRY_RUN|g" /tmp/agent_task.json
<<<<<<< HEAD
          
          echo "task_manifest=$(cat /tmp/agent_task.json | jq -c .)" >> $GITHUB_OUTPUT
      
=======

          echo "task_manifest=$(cat /tmp/agent_task.json | jq -c .)" >> $GITHUB_OUTPUT

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: delegate
        if: ${{ inputs.dry_run != 'true' }}
        name: Execute agent delegation (non-blocking)
        env:
          AGENT_NAME: ${{ steps.init.outputs.agent_name }}
        run: |
          TASK_ID=$(uuidgen | cut -c1-8)
          echo "📬 Queuing agent: $AGENT_NAME (task_id: $TASK_ID)"
<<<<<<< HEAD
          
          echo "task_id=$TASK_ID" >> $GITHUB_OUTPUT
          echo "status=queued" >> $GITHUB_OUTPUT
          
=======

          echo "task_id=$TASK_ID" >> $GITHUB_OUTPUT
          echo "status=queued" >> $GITHUB_OUTPUT

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
          mkdir -p .codex/delegation_tasks
          cat > ".codex/delegation_tasks/${TASK_ID}.json" << 'EOF'
          {
            "task_id": "TASK_ID_PLACEHOLDER",
            "agent": "AGENT_PLACEHOLDER",
            "queued_at": "TIMESTAMP_PLACEHOLDER",
            "status": "queued"
          }
          EOF
<<<<<<< HEAD
          
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
          # Safe substitution
          sed -i "s|TASK_ID_PLACEHOLDER|$TASK_ID|g" ".codex/delegation_tasks/${TASK_ID}.json"
          sed -i "s|AGENT_PLACEHOLDER|$AGENT_NAME|g" ".codex/delegation_tasks/${TASK_ID}.json"
          sed -i "s|TIMESTAMP_PLACEHOLDER|$(date -u +'%Y-%m-%dT%H:%M:%SZ')|g" ".codex/delegation_tasks/${TASK_ID}.json"
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - if: always()
        name: Cache test results (Layer 3)
        uses: actions/cache@v5
        with:
          key: ${{ runner.os }}-pytest-${{ github.sha }}
          path: .pytest_cache/
          restore-keys: |
            ${{ runner.os }}-pytest-
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - if: ${{ inputs.dry_run == 'true' }}
        name: Dry-run output
        env:
          AGENT_NAME: ${{ steps.init.outputs.agent_name }}
          TASK_MANIFEST: ${{ steps.task_manifest.outputs.task_manifest }}
        run: |
          echo "🧪 DRY RUN: Would delegate to $AGENT_NAME"
          echo "   Task manifest: $TASK_MANIFEST"
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Summary
        run: |
          echo "## Agent Delegation Step Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Agent:** ${{ steps.init.outputs.agent_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Dry Run:** ${{ inputs.dry_run || false }}" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** Delegated" >> $GITHUB_STEP_SUMMARY

  coalesce_results:
    if: ${{ always() && inputs.dry_run != 'true' }}
    name: Coalesce Parallel Results
    needs:
      - load_context
      - delegate_agents
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: collect
        name: Collect parallel outputs
        run: |
          python3 << 'PYTHON_EOF'
          import json
          import os
          from pathlib import Path
          from datetime import datetime, timezone

          # Initialize result collection
          results = {
              "generated_at": datetime.now(timezone.utc).isoformat(),
              "total_agents": 0,
              "completed_agents": 0,
              "failed_agents": 0,
              "delegated_tasks": [],
              "merged_output": {},
              "next_actions": []
          }

          # Collect task metadata
          tasks_dir = Path(".codex/delegation_tasks")
          if tasks_dir.exists():
              for task_file in tasks_dir.glob("*.json"):
                  try:
                      task = json.loads(task_file.read_text())
                      results["delegated_tasks"].append(task)
                      results["total_agents"] += 1
                  except Exception as e:
                      print(f"⚠️ Failed to load {task_file}: {e}")

          # Save aggregated results
          output_file = Path(".codex/delegation_results.json")
          output_file.write_text(json.dumps(results, indent=2))

          print(f"✅ Coalesced results from {results['total_agents']} agents")
          print(f"   Output saved to: {output_file}")
          PYTHON_EOF
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Generate delegation summary
        env:
          DELEGATION_MODE: ${{ inputs.delegation_mode || 'parallel' }}
          DELEGATION_COUNT: ${{ needs.load_context.outputs.delegation_count }}
        run: |
          {
            echo "## 🎯 Agent Delegation Summary"
            echo ""
            echo "**Timestamp:** $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            echo "**Mode:** $DELEGATION_MODE"
            echo "**Agents Delegated:** $DELEGATION_COUNT"
            echo ""
<<<<<<< HEAD
            
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
            if [ -f ".codex/delegation_results.json" ]; then
              echo "**Results:** ✅ Coalesced"
              echo ""
              cat .codex/delegation_results.json
            fi
          } | tee /tmp/delegation_summary.md
<<<<<<< HEAD
          
          cat /tmp/delegation_summary.md >> $GITHUB_STEP_SUMMARY
      
=======

          cat /tmp/delegation_summary.md >> $GITHUB_STEP_SUMMARY

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - id: dashboard
        name: Generate dashboard
        run: |
          python3 scripts/ci/rapid_delegation_engine.py 2>/dev/null || echo "ℹ️ Dashboard generation skipped (optional)"
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Summary
        run: |
          echo "## Coalesce Results Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** Results coalesced" >> $GITHUB_STEP_SUMMARY

  finalize:
    if: ${{ always() }}
    name: Finalize Delegation
    needs:
      - load_context
      - delegate_agents
      - coalesce_results
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Report delegation status
        env:
          DELEGATION_MODE: ${{ inputs.delegation_mode || 'parallel' }}
          DELEGATION_COUNT: ${{ needs.load_context.outputs.delegation_count }}
          DRY_RUN: ${{ inputs.dry_run || false }}
          MAX_PARALLEL: ${{ inputs.max_agents || 5 }}
        run: |
          echo "📊 Adaptive Agent Delegation Framework - Status Report"
          echo ""
          echo "✅ Framework Components:"
          echo "  1. Context Loading: ✅ COMPLETE"
          echo "  2. Agent Delegation: ✅ QUEUED ($DELEGATION_COUNT agents)"
          echo "  3. Result Coalescing: ✅ COMPLETE"
          echo ""
          echo "🎯 Delegation Mode: $DELEGATION_MODE"
          echo "📡 Max Parallel: $MAX_PARALLEL"
          echo "🔒 Dry Run: $DRY_RUN"
          echo ""
          echo "💾 Output Locations:"
          echo "  - .codex/delegation_results.json"
          echo "  - .codex/RAPID_DELEGATION_STATUS.md"
          echo "  - .codex/delegation_tasks/*.json"
<<<<<<< HEAD
      
=======

>>>>>>> ae487242 (Session checkpoint complete: Cherry-pick enablement, all phases planning documented, handoff ready)
      - name: Summary
        env:
          DELEGATION_MODE: ${{ inputs.delegation_mode || 'parallel' }}
          DELEGATION_COUNT: ${{ needs.load_context.outputs.delegation_count }}
        run: |
          echo "## Adaptive Agent Delegation Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Status:** ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "📤 **Agents Queued:** $DELEGATION_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "⚙️ **Mode:** $DELEGATION_MODE" >> $GITHUB_STEP_SUMMARY
