name: "🤖 Copilot Review Responder"

# Fires when a pull_request_review is submitted.
# When the Copilot bot posts a review containing "generated N comments", this
# workflow:
#   1. Reacts with 👍 to ALL bot-posted review comments/threads on the PR
#      (so subsequent agent sessions know those items have been seen).
#      Note: A 👍 reaction indicates the comment has been acknowledged by the
#      workflow and is queued for action. Reactions are NOT removed automatically
#      — a coding agent that updates a file should also remove stale 👍 reactions
#      to signal the item needs re-review.
#   2. Posts "@copilot apply changes based on the comments in [this thread](URL)"
#      with the exact review URL (including review ID) so the next Copilot
#      Coding Agent session targets the specific review.
#
# Bot actors targeted: copilot-pull-request-reviewer[bot], copilot[bot],
#   github-copilot[bot], gemini-code-assist[bot], amazon-q[bot],
#   copilot-swe-agent[bot]
#
# Security:
#   - Only fires on PR reviews (not issue comments) to avoid infinite loops.
#   - GROUNDED: workflow only auto-posts the continuation comment when the
#     review explicitly says "generated N comments" (i.e. actionable findings).

on:
  pull_request_review:
    types: [submitted]

concurrency:
  group: copilot-review-responder-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  issues: write

jobs:
  respond-to-review:
    name: "Auto-respond to Copilot review"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Only trigger when:
    #   1. A recognized bot submitted the review, AND
    #   2. The review body contains "generated N comments" (actionable findings)
    if: |
      contains(fromJSON('["copilot-pull-request-reviewer[bot]","copilot[bot]","github-copilot[bot]","gemini-code-assist[bot]","copilot-swe-agent[bot]"]'), github.event.review.user.login) &&
      contains(github.event.review.body, 'generated') &&
      contains(github.event.review.body, 'comments')

    steps:
      - name: "👍 React to and acknowledge bot review comments"
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const reviewId = context.payload.review.id;

            // Bot actors whose comments we acknowledge with 👍
            const BOT_ACTORS = new Set([
              'copilot-pull-request-reviewer[bot]',
              'copilot[bot]',
              'github-copilot[bot]',
              'gemini-code-assist[bot]',
              'copilot-swe-agent[bot]',
              'amazon-q[bot]',
            ]);

            // ── 1. React 👍 to the PR review submission itself ───────────────
            // Use the correct endpoint: createForPullRequestReview (not review comment)
            try {
              await github.rest.reactions.createForPullRequestReview({
                owner, repo, pull_number: pr, review_id: reviewId, content: '+1',
              });
              console.log(`👍 Reacted to review ${reviewId}`);
            } catch (e) {
              console.log(`Could not react to review ${reviewId}: ${e.message}`);
            }

            // ── 2. Fetch all PR review comments and react 👍 to bot ones ──
            let page = 1;
            const acknowledged = [];
            while (true) {
              const { data: comments } = await github.rest.pulls.listReviewComments({
                owner, repo, pull_number: pr, per_page: 100, page,
              });
              if (comments.length === 0) break;
              for (const comment of comments) {
                const actor = comment.user?.login ?? '';
                if (!BOT_ACTORS.has(actor)) continue;

                // Check if we already 👍'd this comment
                let alreadyReacted = false;
                try {
                  const { data: reactions } = await github.rest.reactions.listForPullRequestReviewComment({
                    owner, repo, comment_id: comment.id,
                  });
                  alreadyReacted = reactions.some(
                    r => r.content === '+1' && r.user?.login === 'github-actions[bot]'
                  );
                } catch (e) { /* ignore */ }

                if (!alreadyReacted) {
                  try {
                    await github.rest.reactions.createForPullRequestReviewComment({
                      owner, repo, comment_id: comment.id, content: '+1',
                    });
                    acknowledged.push(comment.id);
                    console.log(`👍 Acknowledged review comment ${comment.id} by ${actor}`);
                  } catch (e) {
                    console.log(`Could not react to comment ${comment.id}: ${e.message}`);
                  }
                }
              }
              page++;
            }

            // ── 3. Also acknowledge regular PR issue comments from bots ───
            page = 1;
            while (true) {
              const { data: issueComments } = await github.rest.issues.listComments({
                owner, repo, issue_number: pr, per_page: 100, page,
              });
              if (issueComments.length === 0) break;
              for (const comment of issueComments) {
                const actor = comment.user?.login ?? '';
                if (!BOT_ACTORS.has(actor)) continue;

                let alreadyReacted = false;
                try {
                  const { data: reactions } = await github.rest.reactions.listForIssueComment({
                    owner, repo, comment_id: comment.id,
                  });
                  alreadyReacted = reactions.some(
                    r => r.content === '+1' && r.user?.login === 'github-actions[bot]'
                  );
                } catch (e) { /* ignore */ }

                if (!alreadyReacted) {
                  try {
                    await github.rest.reactions.createForIssueComment({
                      owner, repo, comment_id: comment.id, content: '+1',
                    });
                    acknowledged.push(comment.id);
                    console.log(`👍 Acknowledged issue comment ${comment.id} by ${actor}`);
                  } catch (e) {
                    console.log(`Could not react to issue comment ${comment.id}: ${e.message}`);
                  }
                }
              }
              page++;
            }

            console.log(`Total acknowledged: ${acknowledged.length} comment(s)`);

      - name: "💬 Post @copilot apply changes from this review"
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const reviewBody = context.payload.review.body ?? '';
            const reviewer = context.payload.review.user.login;
            const reviewId = context.payload.review.id;

            // Extract the "generated N comments" count for the message
            const match = reviewBody.match(/generated\s+(\d+)\s+comments?/i);
            const count = match ? parseInt(match[1], 10) : null;

            // Build the exact review thread URL
            const reviewUrl = `https://github.com/${owner}/${repo}/pull/${pr}#pullrequestreview-${reviewId}`;

            // Only post if the review actually generated comments (count > 0)
            if (count !== null && count === 0) {
              console.log(`Review generated 0 comments — skipping @copilot trigger.`);
              return;
            }

            const countStr = count !== null ? `${count}` : 'some';
            const body = [
              `@copilot apply changes based on the comments in [this thread](${reviewUrl})`,
              ``,
              `> 🤖 **Auto-triggered** by \`${reviewer}\` review (${countStr} comment(s) generated).`,
              `> All bot review comments have been acknowledged with 👍.`,
              `> Please address all review comments and resolve any open threads.`,
            ].join('\n');

            await github.rest.issues.createComment({
              owner, repo, issue_number: pr, body,
            });
            console.log(`@copilot apply posted for review ${reviewId} (${countStr} comments).`);

