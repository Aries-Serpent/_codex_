# ⛔ DISABLED 2026-05-21 — Orphan workflow
#
# This workflow no longer has an active backing implementation. It registered as
# "active" in GitHub Actions (ID: 232765030) because its file existed on a prior
# branch, but the file was absent from main and has 0 lifetime runs.
#
# Creating this stub with no triggers prevents any future accidental execution
# while preserving the workflow name in the Actions history for audit purposes.
#
# Reason:  Orphan — backing file absent from main; 0 lifetime runs.
# Status:  ⛔ Already Disabled (2026-05-21)
# Session: Workflow Portfolio Triage — Archive Review pass

name: cache-health-monitor

on: {}  # no triggers — workflow is permanently disabled

jobs: {}  # no jobs — satisfies YAML schema while preventing execution
