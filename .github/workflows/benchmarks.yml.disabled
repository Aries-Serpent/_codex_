name: Performance Benchmarks
on:
  workflow_dispatch: null
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions: {}
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
    - run: "echo \"disabled \u2014 stub only\""
    timeout-minutes: 60
