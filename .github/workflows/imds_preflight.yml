name: IMDS Pre-Flight Check

on:
  workflow_dispatch:
  pull_request:
    branches: [ 0D_base_, main ]
    paths:
      - ".github/scripts/imds_diagnostic.sh"
      - ".github/workflows/imds_preflight.yml"

jobs:
  imds-preflight:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run IMDS diagnostics (JSON)
        run: |
          bash .github/scripts/imds_diagnostic.sh --json || true
          if [ ! -f diagnostic_results.json ]; then
            echo "JSON summary missing; failing pre-flight."
            exit 1
          fi
          cat diagnostic_results.json

      - name: Gate on status == ok
        run: |
          STATUS=$(jq -r '.status' diagnostic_results.json)
          echo "Status: $STATUS"
          if [ "$STATUS" = "ok" ]; then
            echo "IMDS pre-flight passed."
            exit 0
          elif [ "$STATUS" = "remediation-recommended" ]; then
            echo "IMDS status indicates remediation is recommended (STATUS=$STATUS). Please review and remediate as needed."
            exit 1
          elif [ "$STATUS" = "remediation-failed" ]; then
            echo "IMDS remediation attempt failed (STATUS=$STATUS). Immediate attention required!"
            exit 1
          else
            echo "IMDS status unknown or error (STATUS=$STATUS). Please investigate."
            exit 1
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: imds-diagnostics
          path: |
            diagnostic_results.txt
            diagnostic_results.json
