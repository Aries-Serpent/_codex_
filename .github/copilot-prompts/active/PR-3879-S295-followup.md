# Follow-Up Prompt — PR #3879 · Session S295 · 2026-04-06

> **Status:** ✅ S295 Complete  
> **HEAD SHA:** `6a0e9743d`  
> **Branch:** `0D_base_`  
> **Commit:** `deps(s295): cherry-pick 6 open dependabot PRs + PR lifecycle gaps fixed + unused-import audit clean`

---

## ✅ S295 Completed

| Task | Status |
|------|--------|
| Duplicate `env:` fix in `process-variable-intents.yml` | ✅ `23f2350` |
| Cherry-pick 10 dependabot open PRs (#3886–#3895) | ✅ `6a0e974` |
| ruff F401 unused-import audit | ✅ 0 violations |
| PR lifecycle audit vs `docs/ci/PR_LIFECYCLE.md §14.5` | ✅ all gaps fixed |
| CHANGELOG `### Fixed (S295)` entry | ✅ |
| Accountability report S295 session entry | ✅ |
| Reply to 4 blocking `comment_new` items | ✅ 4190121885, 4190122473, 4190129471, 4192824707 |
| Code Review + CodeQL scan | ✅ both PASS |
| Cognitive brain metadata updated | ✅ `.codex/cognitive_brain/metadata.json` |

---

## 🔴 Priority 1 — Next Session MUST Complete

1. **Verify `process-variable-intents.yml` CB App token run**
   - Confirm the merged `env:` block (GH_TOKEN + DRY_RUN in single mapping) allows the workflow
     to successfully read intent files from `.codex/pending_ops/` and create variables.
   - Check workflow run history for any new run triggered by the `6a0e974` push.

2. **Monitor comment-review-gate on commit `6a0e974`**
   - All 4 blocking comments replied to → gate should clear on next scan.
   - If gate re-fires: check `scripts/ci/check_pr_comments.py` for any new bot comments.

3. **Tighten mypy baseline**
   ```bash
   python scripts/ci/mypy_baseline.py --update
   ```
   Current: 104 errors actual, 386 baseline → 282 below baseline. Run `--update` to lock in.

---

## 🟡 Priority 2 — Validation Tasks

4. **Close 10 dependabot PRs** (requires `CODEX_MASTER_KEY`)
   All 10 open dependabot PRs (#3886–#3895) have been consolidated into `0D_base_`.
   They can now be closed with a comment referencing this PR.
   ```bash
   # For each PR number: 3886 3887 3888 3889 3890 3891 3892 3893 3894 3895
   GH_TOKEN="$CODEX_MASTER_KEY" gh pr close <number> \
     --repo Aries-Serpent/_codex_ \
     --comment "Consolidated into 0D_base_ via PR #3879 commit 6a0e974. Closing."
   ```

5. **Verify dependabot cherry-picks are correct**
   Run against each changed requirement file to ensure versions are consistent:
   ```bash
   grep -E "^(huggingface-hub|fastapi-cli|pyparsing|sqlalchemy|pandas|transformers)==" \
     requirements/lock.txt requirements/lock-eval.txt requirements/lock-ml.txt \
     requirements-eval.txt requirements-ml-cpu.txt requirements/base.txt
   ```
   Expected: huggingface-hub==1.9.0, fastapi-cli==0.0.24, pyparsing==3.3.2,
             sqlalchemy==2.0.49, pandas==3.0.2, transformers==5.5.0

---

## 🟢 Priority 3 — Enhancement

6. **Fix `pr-cost-check.yml` false-positive RED alerts**
   Root cause: workflow scans all candidate workflows regardless of WEC checkbox state.
   Fix: cross-reference WEC-enabled list before calculating cost tier.
   File: `.github/workflows/pr-cost-check.yml`

7. **Fix WEC duplicate comment accumulation**
   Root cause: `workflow-execution-gate.yml` posts a new comment on every `pull_request` push
   instead of upserting via the existing `<!-- wec-execution-plan:{pr} -->` marker.
   Fix: add upsert logic on `pull_request` trigger (currently only on `pull_request_review`).

8. **Implement dependabot→0D_base_ auto-cherry-pick workflow**
   Create `.github/workflows/dependabot-consolidate.yml` that:
   - Triggers on `pull_request` opened with `dependabot/**` head branch
   - Extracts only the lock file version bump lines
   - Cherry-picks them into `0D_base_` automatically
   - Posts a comment on the dependabot PR linking the consolidated commit

---

## 📋 End-of-Session Checklist (repeat at session start)

```bash
# §0 pre-session context
python scripts/ci/pre_session_context.py --repo Aries-Serpent/_codex_ --pr 3879

# Fix verification
python -m ruff check src/ tests/ --fix
python scripts/ci/mypy_baseline.py --require-baseline
python scripts/ci/auto_fix_common_issues.py --check-only
/home/runner/work/_codex_/_codex_/.venv_agent/bin/actionlint .github/workflows/*.yml

# Unused imports (must be 0)
python -m ruff check src/ tests/ --select F401
```

---

## 🧠 Cognitive Brain Status

| Key | Value |
|-----|-------|
| Session | S295 |
| Status | ✅ active |
| PR | #3879 |
| Branch | `0D_base_` |
| HEAD | `6a0e9743d` |
| ruff | PASS |
| mypy | PASS (104/386) |
| actionlint | PASS |
| F401 | PASS (0) |
| code_review | PASS |
| codeql | PASS |

---

*Generated by Copilot S295 · 2026-04-06T14:40Z · PR #3879*
