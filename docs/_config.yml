# DISABLED — This Jekyll configuration file is no longer in use.
#
# GitHub Pages for this repository is deployed EXCLUSIVELY via the
# MkDocs workflow: .github/workflows/pages-mkdocs.yml
#
# The site is built with MkDocs Material and deployed to:
#   https://aries-serpent.github.io/_codex_/
#
# Do NOT re-enable this file. The MkDocs source is docs/ and the
# built site is uploaded via actions/upload-pages-artifact in
# pages-mkdocs.yml.
#
# Original Jekyll config archived at: .codex/archive/jekyll_config_disabled.yml
