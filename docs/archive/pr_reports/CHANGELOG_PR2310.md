# Changelog for PR #2310 (monolithic)

- Add decoder/extractor for Phase-A validator snapshot (base64+gz)
- Add baseline generation helper and wiring (--generate-baseline)
- Add stable manifest helper for deterministic tests
- Add permissive JSON schema and validator
- Add coverage ingest stub and end-to-end coverage test
- Add requirements-dev and conftest to stabilize pytest collection
- Add pre-commit config and sandbox runner for local automation
