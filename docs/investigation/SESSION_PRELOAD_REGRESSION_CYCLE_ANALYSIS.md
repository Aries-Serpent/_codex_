# Session Preload Evidence-Based Fix Strategy\n## Critical Discovery: Commit Regression Cycle Analysis\n\n**Generated:** 2026-06-08 | **Status:** DETAILED INVESTIGATION  \n**Objective:** Analyze the complete history of attempted fixes and identify the ONLY proven working pattern\n\n---\n\n## Commit History Pattern Analysis\n\nFrom the recent 50-commit history of `.github/workflows/copilot-setup-steps.yml`, a clear regression cycle emerges:\n\n| Commit | Author | Pattern | Status | Notes |\n|--------|--------|---------|--------|-------|\n| `2e59997` | mbaetiong | Method B (block scalar `if !`) | ✅ DEPLOYED | chore: progress plan — human commit |\n| `c467904` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | Agent auto-fix reverted the fix |\n| `3333f2b` | mbaetiong | Method B (re-applied) | ✅ DEPLOYED | Human restored the fix |\n| `7b79f55` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | Agent broke it again |\n| `4ed9775` | mbaetiong | Method B (re-applied again) | ✅ DEPLOYED | Pattern: human fixes, agent breaks |\n| `7badb39` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | **4th regression cycle** |\n| `502c1bc` | mbaetiong | Method B (re-applied) | ✅ DEPLOYED | Cycle repeats |\n| `4c093a6` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | 5th time |\n| `f8a1a66` | mbaetiong | Method B (human fix) | ✅ DEPLOYED | |\n| `91a2b63` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | 6th regression |\n| `5271018` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | 7th regression |\n| `978190` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | 8th regression |\n| `c7be338` | mbaetiong | Method B (human fix) | ✅ DEPLOYED | Pattern is **100% consistent** |\n| `8b73a70` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | |\n| `54faf7d` | Copilot (bot) | **REVERTED** to flow scalar | ❌ BROKEN | |\n\n### The Clear Pattern\n\n**Human commits:** Apply Method B (block scalar with `if !` syntax)  \n**Agent commits:** IMMEDIATELY revert to broken flow scalar form\n\n**This is NOT a \"fix works sometimes\" situation.**  \nThis is **\"fix works, but agents keep breaking it\"**.\n\nThe test `test_copilot_setup_steps_session_preload_block_intact()` exists BECAUSE of this cycle — it was added to catch regressions.\n\n---\n\n## Why Method B Gets Reverted by Agents\n\n### What the Agents See\n\nWhen a Copilot agent encounters a failing CI step, it sees:\n\n```yaml\n- name: \"🧠 Session Context Pre-load\"\n  continue-on-error: true\n  run: |\n    if ! python3 .github/scripts/session_preload.py; then\n      echo \"⚠️ failed (non-blocking)\"\n    fi\n```\n\nThe agent's training teaches it:\n- \"Flow scalars are natural for bash fallbacks: `command || echo error`\"\n- \"Multi-line run blocks are verbose; can be simplified\"\n- \"This looks like it can be condensed\"\n\nSo it \"optimizes\" to:\n\n```yaml\nrun: python3 .github/scripts/session_preload.py || {\n  echo \"⚠️ failed (non-blocking)\"\n}\n```\n\n**The agent doesn't know:**\n- This syntax breaks YAML parsing\n- The guard comment explicitly forbids this change\n- It creates silent failures\n\n**Even with the guard comment in place, agents revert it.**\n\n---\n\n## The REAL Solution: NOT Method B, but Method D\n\n### Why Method D (proven in Session Access Probe) Works\n\nSessionAccess Probe uses this pattern and it's NEVER been broken by agents:\n\n```yaml\nrun: |\n  echo \"::group::Session Access Probe\"\n  python3 scripts/ci/session_access_probe.py || {\n    echo \"⚠️ Access probe failed (non-blocking)\"\n    echo \"ACCESS_REST=unknown\" >> \"$GITHUB_ENV\"\n  }\n  echo \"::endgroup::\"\n```\n\n### Why Agents Don't Break This\n\n1. **The `||`  is wrapped in a block scalar (`run: |`)**\n   - Agents see it as a complete multi-line block\n   - They don't try to \"simplify\" it\n   - YAML parsing never breaks\n\n2. **There's actual work happening** (setting env vars, grouping logs)\n   - Looks intentional, not condensable\n   - Agents don't touch it\n\n3. **The guard comment would help, but Method D's structure itself prevents breaking**\n   - Even without guard comment, Method D is harder to break\n\n---\n\n## Hypothesis: Why Method D Resists Agent Refactoring\n\n**Method D structure triggers different heuristics:**\n\n```yaml\nrun: |\n  <multi-line block>\n  python3 script.py || {\n    <fallback handler>\n  }\n  <more work>\n```\n\nAgent sees: \"This is a structured shell workflow, not a simple command\"\nAgent decision: \"Leave it alone — it's doing real things\"\n\n**Method B structure triggers simplification heuristics:**\n\n```yaml\nrun: |\n  if ! python3 script.py; then\n    <fallback>\n  fi\n```\n\nAgent sees: \"This can be a simple || fallback\"\nAgent decision: \"Simplify this to one line\"\n\n---\n\n## Recommended Action: Use Method D + Additional Guards\n\n### Implementation (Copy Session Access Probe Pattern)\n\n```yaml\n- name: \"🧠 Session Context Pre-load (memory + policy + accountability + PDA)\"\n  id: session_preload\n  continue-on-error: true\n  run: |\n    echo \"::group::Session Context Pre-load\"\n    python3 .github/scripts/session_preload.py || {\n      echo \"⚠️ session_preload.py failed (non-blocking) — agent will operate without preloaded context\"\n      echo \"SESSION_PRELOAD_STATUS=failed\" >> \"$GITHUB_ENV\"\n    }\n    echo \"::endgroup::\"\n```\n\n### Why This Works\n\n1. ✅ **Block scalar wrapper (`run: |`)** — YAML parses correctly\n2. ✅ **Grouped output (`::group::`)** — Looks intentional, harder to simplify\n3. ✅ **Env var export** — Agent sees it as doing real work\n4. ✅ **Proven pattern** — Access Probe uses it, never regressed\n5. ✅ **No agent rewrites it** — The structure resists \"optimization\"\n\n### Why This Beats Method B\n\n| Aspect | Method B | Method D |\n|--------|----------|----------|\n| **Syntax** | Shell guard `if !` | Flow scalar `\\|\\|` |\n| **YAML parsing** | ✅ Safe | ✅ Safe |\n| **Agent refactoring risk** | ⚠️ HIGH (explicit condition looks \"optimizable\") | ✅ LOW (structured block looks intentional) |\n| **Proven history** | ❌ Regressed 8+ times | ✅ Never regressed (Access Probe) |\n| **Guard comment helps** | ⚠️ Ineffective (ignored 8+ times) | ✅ Unnecessary (structure prevents breaking) |\n\n---\n\n## Implementation Plan: Method D Deployment\n\n### Phase 1: Deploy Method D (< 1 hour)\n\n1. **Create branch:** `chore/session-preload-method-d-deployment`\n2. **Update step** to Method D (copy from Session Access Probe)\n3. **Keep all guards:** Guard comment + architectural test\n4. **Deploy to PR**\n5. **Trigger Copilot session**\n6. **Verify logs show preload output** (proves it's executing)\n7. **Verify logs DON'T show agent trying to refactor it**\n\n### Phase 2: Validate (1 session, ~30 min)\n\n- [ ] Copilot session completes without preload output (no silent failure)\n- [ ] Preload output appears in `::group::` section\n- [ ] No subsequent agent commits revert the syntax\n- [ ] Tests pass\n\n### Phase 3: Lock Against Future Regressions\n\n- [ ] Activate architectural test (already exists)\n- [ ] Add validation script to CI (already exists)\n- [ ] Document in agent rules: Method D is canonical, Method B was regressed\n\n---\n\n## Critical Insight\n\n**The issue is NOT that Method B is wrong.**  \n**The issue is that agents keep \"fixing\" Method B by reverting it.**\n\n**Method D doesn't have this problem because its structure is less \"optimizable.\"**\n\nThis is a practical consideration for agentic-managed repos: Pick the pattern that **resists agent refactoring**, not just the \"theoretically correct\" one.\n\n---\n\n## Action Items\n\n1. **Acknowledge:** Method B has regressed 8+ times; Method D has never regressed\n2. **Decision:** Deploy Method D (proven pattern)\n3. **Timeline:** Can be done today (copy/paste from Access Probe)\n4. **Verification:** Single Copilot session validates it\n5. **Lock:** Tests + guard comment prevent regressions\n\n**Do NOT pursue Method B again.** The regression cycle is explicit and repeated.\n"