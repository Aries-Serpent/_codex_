# ============================================================================
# DEPRECATED: Use configs/experiments/default.yaml instead
# See misc/repo-owner-review/MIGRATION_GUIDE.md for migration instructions
# Removal scheduled: v2.0.0 (Q2 2026)
# ============================================================================
name: offline-default
git_hash: ${oc.env:CODEX_GIT_HASH,unknown}
config_version: conf/config.yaml
data_version: ${data.dataset_path}
run_id: null
