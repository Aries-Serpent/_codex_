{# Audit Trend Report Template - v1.5.3 #}
# 📊 Audit Trend Report

**Generated:** {{ timestamp }}
**Repository:** {{ repo_name }}
**Pipeline Version:** {{ version }}
**Analysis Period:** {{ period_start }} → {{ period_end }}

---

## Executive Summary

| Metric | Value | Trend |
|--------|-------|-------|
| **Average Score** | {{ "%.3f"|format(avg_score) }} | {{ avg_trend }} |
| **Capabilities ≥ 0.85** | {{ high_count }}/{{ total_count }} | {{ high_trend }} |
| **Regressions Detected** | {{ regression_count }} | {{ regression_trend }} |
| **Coverage (if enabled)** | {{ coverage_pct|default('N/A') }} | {{ coverage_trend|default('—') }} |

---

## Score Distribution

```
High (≥0.85):   {{ '█' * high_bar }}{{ '░' * (20 - high_bar) }} {{ high_count }} ({{ high_pct }}%)
Medium (0.70+): {{ '█' * med_bar }}{{ '░' * (20 - med_bar) }} {{ medium_count }} ({{ med_pct }}%)
Low (<0.70):    {{ '█' * low_bar }}{{ '░' * (20 - low_bar) }} {{ low_count }} ({{ low_pct }}%)
```

---

## Trend Sparklines (Last 20 Runs)

| Capability | Current | Trend | Sparkline |
|------------|---------|-------|-----------|
{% for cap in capabilities|sort(attribute='id') -%}
| {{ cap.id }} | {{ "%.3f"|format(cap.score) }} | {{ cap.trend_indicator }} | `{{ cap.sparkline }}` |
{% endfor %}

---

## Regressions

{% if regressions %}
| Capability | Previous | Current | Δ | Severity | Root Cause |
|------------|----------|---------|---|----------|------------|
{% for reg in regressions -%}
| {{ reg.capability_id }} | {{ "%.3f"|format(reg.previous) }} | {{ "%.3f"|format(reg.current) }} | {{ "%.3f"|format(reg.delta) }} | {{ reg.severity }} | {{ reg.weakest_component }} |
{% endfor %}
{% else %}
✅ No regressions detected in this period.
{% endif %}

---

## Improvements

{% if improvements %}
| Capability | Previous | Current | Δ | Key Driver |
|------------|----------|---------|---|------------|
{% for imp in improvements -%}
| {{ imp.capability_id }} | {{ "%.3f"|format(imp.previous) }} | {{ "%.3f"|format(imp.current) }} | +{{ "%.3f"|format(imp.delta) }} | {{ imp.best_component }} |
{% endfor %}
{% else %}
ℹ️ No significant improvements in this period.
{% endif %}

---

## Component Analysis

### Weakest Components (Across All Capabilities)

| Component | Avg Score | Trend | Recommendation |
|-----------|-----------|-------|----------------|
{% for comp in weakest_components -%}
| {{ comp.name }} | {{ "%.3f"|format(comp.avg) }} | {{ comp.trend }} | {{ comp.recommendation }} |
{% endfor %}

---

## Historical Trend Summary

{% if trend_summary %}
### Score History

| Date | Avg Score | High Count | Regressions |
|------|-----------|------------|-------------|
{% for entry in trend_summary -%}
| {{ entry.date }} | {{ "%.3f"|format(entry.avg_score) }} | {{ entry.high_count }} | {{ entry.regression_count }} |
{% endfor %}
{% endif %}

---

## Next Steps

{% for step in recommendations %}
{{ loop.index }}. {{ step }}
{% endfor %}

---

## Appendix: Full Capability Matrix

<details>
<summary>Click to expand full capability matrix</summary>

| ID | Score | F | C | T | S | D |
|----|-------|---|---|---|---|---|
{% for cap in capabilities|sort(attribute='id') -%}
| {{ cap.id }} | {{ "%.3f"|format(cap.score) }} | {{ "%.2f"|format(cap.components.functionality|default(0)) }} | {{ "%.2f"|format(cap.components.consistency|default(0)) }} | {{ "%.2f"|format(cap.components.tests|default(0)) }} | {{ "%.2f"|format(cap.components.safeguards|default(0)) }} | {{ "%.2f"|format(cap.components.documentation|default(0)) }} |
{% endfor %}

</details>

---

*Report generated by Audit Pipeline v{{ version }}*
