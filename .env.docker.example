# Example environment variables for local Docker runs
# Copy to .env and customize as needed; do NOT commit secrets.
PORT=8000
LOG_LEVEL=info
# Optional runtime toggles
APP_MODULE=src.codex.api.app:app
PRESTART_CMD=
PYTHONUNBUFFERED=1
