version: "3.8"
services:
  codex:
    image: "codex:local"
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: "no"
    command: ["uvicorn", "src.codex.api.app:app", "--host", "0.0.0.0", "--port", "8000", "--log-level", "info"]
